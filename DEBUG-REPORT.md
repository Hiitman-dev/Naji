# 🐞 گزارش دیباگ «ناجی» (bargh-monitor)

> تاریخ دیباگ: ۱۴۰۴/۰۶/۰۷ — کل پروژه به‌صورت ایستا (compile + review) و پویا
> (smoke test ۱۹ بررسی + ۱۰ سناریوی شبیه‌سازی‌شده روی رویدادهای واقعی Qt) بررسی شد.

---

## ✅ خلاصه نتیجه

| بررسی | وضعیت |
|---|---|
| کامپایل تمام ماژول‌ها | ✔ بدون خطای سینتکس |
| تست دود `test_smoke.py` | ✔ ۱۸/۱۹ پاس (تنها «مسیر شبکه» به‌دلیل بلاک IP غیرایرانی سرور، در محیط تست ماک شده) |
| سناریوهای بحرانی (لغو ورود، کرش خروج، تنظیمات خراب، شمارش معکوس) | ✔ همه بعد از رفع، پاس |
| رندر UI در هر دو تم | ✔ بدون رگرسیون بصری |

---

## بازطراحی نسخه‌ی ۵٫۰٫۰ — «بنفشِ نیمه‌شب» (پریمیوم 2.5D)

> درخواست کاربر: «آیکون‌ها خیلی چیپه»، «کل برنامه حس 2.5D داشته باشه» + بریفت کامل
> پریمیوم (بوم `#0D0F14`، اکسنت `#7B5CFF`، آیکون‌های Lucide، شیشه‌ی اکریلیک، چیدمان مشخص).
> هر دو موتورِ «حسِ ارزان» با عدد و قفلِ تست برطرف شد — هیچ تصمیم حدسی در کار نبود.

### ۱) ریشه‌یابی «چیپ بودنِ آیکون‌ها» — دو عامل واقعی، دو رفع قطعی

**عامل اول — لکه‌ی پرش نرم:** هر گلیفِ قبلی یک لایه‌ی `fill` با آلفای پایین
پشتِ خطوطش داشت (`@SOFT@`). در سایزهای کوچک (۱۶–۲۲px) این لکه‌ها مثل دودِ
هم‌رنگ دورِ خط می‌نشیند و آیکون را «لکه‌دار/چیپ» نشان می‌دهد. **رفع:** هر ۲۸
گلیف حذف و با هندسه‌ی واقعی Lucide (ISC) بازنویسی شد — خطِ خالص، بدون هیچ
fill. قفلِ تست: `fill="@SOFT@"` در هیچ قالبی مجاز نیست (`test_v500`).

**عامل دوم — وزنِ ناهمگون:** قبلاً وزن خط بین `۱٫۴` تا `۲٫۲` نوسان داشت
(هر گلیف سلیقه‌ای). **رفع:** همه‌ی مسیرها با `stroke-width="1.75"` ثابت
+ `stroke-linecap/join="round"` نوشته شدند. قفلِ تست: رجکس روی همه‌ی
قالب‌ها — هر `stroke-width` باید دقیقاً ۱٫۷۵ باشد و تعداد stroke ها با
width ها برابر؛ به‌علاوه رندرِ واقعیِ هر ۲۸ گلیف و سنجش «جوهر» (پیکسل‌های
نیمه‌شفاف) تا هیچ SVG شکسته‌ای بی‌صدا خالی نماند.

### ۲) پالت — بریفت کاربر عیناً اعمال شد

`bg_top=#0d0f14`، `bg_bottom=#090b0f` (نمونه‌ی پیکسلی رندر: ۸,۱۰,۱۳ —
خانواده‌ی بریفت)، `accent=#7b5cff`، `hover=#8f75ff`، `pressed=#6a4be0`،
`ok=#34d399`، هاله `123,92,255`. تم روشن صدف ماند و فقط اکسنت/گرادیان‌ها
به بنفشِ `#6d50f0` هم‌خانواده رفتند تا «اختلاف رنگ بخش‌ها» در هر دو تم
یک زبان داشته باشد. نورِ بومِ تیره (تینتِ مایکا) فامِ بنفش با آلفای ۲۲.

### ۳) حس 2.5D — سه لایه‌ی عمقِ اندازه‌گیری‌شده

- **اِلیویشن کارت:** پرش از تک‌رنگ به گرادیان عمودی (بالای کارت `lighter(112%)`
  در تیره، کف با آلفای کمتر) — تفکیک سطح با روشنایی، مطابق متریال.
- **لبه‌ی ضخامت:** گرادیانِ سایه‌ی داخلی ۱۶px در کفِ هر کارت (آلفای ۳۰ تیره /
  ۱۲ روشن) — کارت مثل اسلبِ سرِپا از بوم جدا می‌شود.
- **نورِ فعال:** هاله‌ی شعاعی بنفش (آلفا ≤۶۴×tint) زیرِ قرصِ ناوبریِ فعال +
  هاله‌ی دکمه‌ی «به‌روزرسانی فوری» (blur=۳۶/alpha=۱۱۰). سایه‌های کارت‌ها
  عمیق‌تر شدند (هیرو: blur=۴۸/alpha=۸۰/dy=۱۸).
- **شیشه‌ی اکریلیک:** سطح تیره `rgba(26,29,41,0.72)` — بوم از پشت کمی
  می‌گذرد؛ تفکیک پیکسلی کارت/بوم در رندر واقعی: Δ=۲۰ تیره / Δ=۳۲ روشن.

### ۴) چیدمان داشبورد — مطابق بریفت، با گاردِ داده

نوار پایینِ تازه: خلاصه (نقطه‌های رنگی + «۲ مورد در راهه · ۱ مورد امروز ·
پایش ۲ قبض») در سمتِ شروع و دکمه‌ی ۵۲px در انتهای ردیف — خلاصه از کف کارت
خاموشی‌ها به این‌جا منتقل شد تا تکرار نشود. **چیپ موقعیت سربرگ** کاملاً
داده‌محور است: فقط آدرسِ واقعیِ نزدیک‌ترین خاموشیِ آینده را از همان
`outage_addr` هیرو می‌خواند و بدون داده کلاً `setVisible(False)` — برنامه
هیچ‌وقت آدرسِ ساختگی (مثل «بلوار امام»ی بریفت) نشان نمی‌دهد. شمارشِ هیرو
سقفِ قلمش از ۳۸ به ۴۴px رفت و کارت بلندتر شد (۲۳۶px).

### ۵) راستی‌آزمایی

- `test_v500.py` — ۳۶ قفل: پالت امضا، یکنواختی ۱٫۷۵، بدون لکه‌ی نرم،
  جوهرِ همه‌ی گلیف‌ها، اِلیویشن/ضخامت/هاله، نوار پایین، چیپ موقعیت
  (داده‌دار/خالی)، هاله‌ی بنفش با نمونه‌ی پیکسلی رندر واقعی، i18n دو زبان.
- `test_v460` / `test_v470` / `test_v480` — با پالتِ نو هماهنگ شدند: ALL PASS.
- اسموک ۱۸/۱۹ (فقط بلاک همیشگیِ شبکه‌ی برق‌من)، `diag_tabs` ۴/۴،
  `accent_sync`، `lang_purity`، `faen_icon` ۱۱/۱۱، `ui_interaction` — پاس.
- رندر `download/ui_v500/`: ۱۷ صحنه — دو تم × چهار صفحه، سربرگ/نوار پایینِ
  کلوزآپ، EN هر دو تم، اکسنت آبی/زرد ویندوز، پنجره‌ی باریک ۷۶۰px، سنجش
  تفکیک و خانواده‌ی بوم. بازبینی بصری: هر دو تم + EN + اکسنت‌ها سالم.

---

## دیباگ نسخه‌ی ۳.۱ — «عملگرها کار نمی‌کنند» (Aura Glass)

> تاریخ: پس از بازطراحی Aura Glass. این بار علاوه بر بازبینی ایستا، **تست تعامل**
> جدید (`ui_interaction_test.py`) اضافه شد که با شبیه‌سازی کلیک واقعی ماوس
> (QTest) روی تک‌تک دکمه‌ها، سوییچ‌ها، سگمنت‌ها، کامبو و اسپین‌باکس‌ها،
> «کنترل‌های مرده» را سیستماتیک پیدا می‌کند — ۱۷/۱۷ پاس.

### 🔴 باگ A — هر چهار دکمه‌ی پنجره‌ی هشدار کاملاً بی‌عملگر بودند (بحرانی)

**فایل:** `warn_dialog.py`

**شرح:** در بازنویسی بصری v3، اتصال‌های `clicked.connect` چهار دکمه‌ی
«خاموش کردن / حالت خواب / خواب زمستانی / نادیده گرفتن» جا افتاده بود.
کاربر روی هر دکمه‌ای که کلیک می‌کرد هیچ اتفاقی نمی‌افتاد و پنجره تا پایان
شمارش معکوس باز می‌ماند و بعد کنش پیش‌فرض را اجرا می‌کرد — یعنی دقیقاً
لحظه‌ای که کاربر می‌خواست جلوی خاموشی را بگیرد، برنامه بی‌پاسخ بود.

**رفع:** اتصال دکمه‌ها به `_exec(key)` مطابق نسخه‌ی ۲ بازگردانده شد.
**تأیید:** کلیک واقعی هر ۴ دکمه با QTest؛ کنش power ماک‌شده و فراخوانی‌ها assert شد.

### 🔴 باگ B — آیکون صاعقه (لوگوی برنامه) خالی رندر می‌شد

**فایل:** `icons.py`

**شرح:** مسیرِ خطی SVG صاعقه دو ویژگی `fill` داشت (`fill="none"` صریح +
`fill="none"` داخل `_S`). ویژگی تکراری در XML خطای پارس کامل SVG می‌دهد؛
`QSvgRenderer` بدون هیچ پیامی هیچ رندر نمی‌کرد. نتیجه: لوگوی هدر، چیپ
کاشی «قطعی پیش‌رو» و هرجایی که گلیف bolt بود خالی بود.

**رفع:** `fill="none"` تکراری حذف شد + اسکن خودکار هر ۲۲ آیکون (شمارش پیکسل)
اضافه شد به روال تست.
**تأیید:** `BROKEN ICONS: none`.

### 🟠 باگ C — سوییچ اتواستارت فقط در یک ناحیه‌ی باریکِ نامرئی کلیک می‌خورد

**فایل:** `widgets.py`

**شرح:** `Switch` زیرکلاس `QCheckBox` است و ظاهر ۵۰×۳۰ می‌کشد، اما
ناحیه‌ی کلیکِ پیش‌فرض QCheckBox فقط مستطیل indicator استاندارد (~۱۸px در
لبه‌ی ویجت) است. کلیک روی میانه‌ی ریل — جایی که کاربر عادت دارد کلیک کند —
بی‌عملگر بود.

**رفع:** بازنویسی `hitButton()` برای پوشش کل ریل.
**تأیید:** QTest.mouseClick روی مرکز ریل → تغییر وضعیت و ذخیره.

### 🟠 باگ D — فلش کامبوباکس به PNG موقتِ %TEMP% وابسته بود

**فایل:** `theme.py`, `widgets.py`

**شرح:** فلش کامبو به‌صورت PNG در `%TEMP%/naji_assets` کش و از طریق
`url()` داخل QSS تزریق می‌شد. روی سیستم‌هایی که نام کاربر ویندوزی فاصله
یا حروف غیرلاتین دارد، پارس `url()` می‌شکند و فلش (و بسته به موقعیت،
قواعد بعدی QSS) از دست می‌رود — رندر UI روی آن سیستم‌ها خراب می‌شد.

**رفع:** کلاس `GlassCombo` — فلش مستقیماً در `paintEvent` و هم‌رنگ تم رسم
می‌شود؛ مکانیزم PNG موقت و `_ensure_arrow_assets` کامل حذف شد. پاپ‌آپ
کامبو و تولتیپ هم به رنگ توپر تغییر کرد تا گوشه‌ی سیاهِ پنجره‌های سرِپا
نسازند.
**تأیید:** رندر آفسکرین؛ شِوران در هر دو تم و RTL سر جای خودش.

### 🟠 باگ E — فلش‌های QSpinBox نامرئی بودند (کنترل «خراب» دیده می‌شد)

**فایل:** `theme.py`, `widgets.py`

**شرح:** در v3 فلش‌های بالا/پایین اسپین با QSS عرض صفر گرفته بودند.
دکمه‌ها کلیک می‌خوردند اما هیچ بازخورد بصری نداشتند؛ کاربر کنترل را خراب
می‌بیند.

**رفع:** کلاس `GlassSpin` — شِوران‌ها در `paintEvent` رسم می‌شوند و روی
هاور روشن‌تر می‌گردند. (در تست: کلیک روی مختصات واقعی subControl → گام +۱/−۱)

### 🟡 باگ F — در لپ‌تاپ‌های ۷۶۸ پیکسلی دکمه‌های پایین از صفحه بیرون می‌زدند

**فایل:** `main_window.py`, `theme.py`

**شرح:** حداقل ارتفاع پنجره ۸۸۰px بود؛ روی صفحه‌های کوتاه، ردیف دکمه‌های
«به‌روزرسانی فوری / خروج از حساب» زیر لبه‌ی صفحه قرار می‌گرفت و عملاً
غیرقابل کلیک بود. فشرده‌سازی چیدمان (~۸۰px) + حداقل اندازه‌ی ۶۶۰×۶۰۰ +
**QScrollArea شفاف** به‌عنوان صنامه: اگر صفحه کوتاه باشد محتوا اسکرول
می‌شود و هیچ کنترلی هرگز از دسترس خارج نمی‌شود.

### 🟡 باگ G — دو ترکیدگی پنهانِ مرتبط با رندر (سیگفالت خروج)

**فایل:** `widgets.py`

**شرح:** در PySide6، `QStyle.SC_ComboBoxDropDown` وجود ندارد (نام درست
`SC_ComboBoxArrow` است) و `int()` روی `SubControl` کار نمی‌کند. استثنای
داخل `paintEvent` باعث می‌شد `QPainter` باز بماند
(«QPaintDevice: Cannot destroy paint device that is being painted») و در
خروج سیگفالت رخ دهد. هر دو paintEvent جدید با گارد + try/except ایمن شدند.

### 🟡 باگ H — بیلد exe فقط ۴ فونت را باندل می‌کرد

**فایل:** `Naji.spec`, `build_exe.bat`

**شرح:** `datas` فقط ۴ فونت وزیرمتن را شامل بود؛ در بیلد نهایی، استعداد/
شبنم/اسپیس‌گروتسک موجود نبودند و تایپوگرافی چهارسدایی Aura Glass به
یک‌فونتی می‌می‌افتاد. حالا هر TTF داخل `assets/fonts` خودکار باندل می‌شود
+ `PySide6.QtSvg` به hiddenimports اضافه شد.

### 🟡 بهبود I — فشار CPU انیمیشن‌ها

**فایل:** `widgets.py`

**شرح:** بوم شفق در هر تیک (۲۰fps) گرادیان + ~۹۰ کاشی گرِین را از نو
می‌کشید و HeroCard در هر فریمِ شیمر مدار SVG را دوباره رندر می‌کرد.
حالا گرادیان+گرِین در یک لایه‌ی پایه‌ی کش‌شده (بازسازی فقط با تغییر
اندازه/تم) و مدار هیرو در QPixmap کش می‌شوند. مصرف CPU انیمیشن پس‌زمینه
به‌شکل محسوس پایین آمد و احتمال فلیکر روی سیستم‌های ضعیف کم شد.

### 🟡 بهبود J — مقاوم‌سازی

- `storage.set_autostart` حالا هر استثنایی (نه فقط OSError) را به پیام
  خطا تبدیل می‌کند تا خرابی رجیستری برنامه را نیندازد.
- سیگنال `toggled` سوییچ با گارد `_identity_applied` — ثبت بی‌دلیلِ
  اتواستارت در شروع برنامه حذف شد.
- `test_smoke` و سناریوها ۷/۷ و ۴/۴ پاس؛ تنها چکِ شبکه‌ی زنده (بلاک IP
  غیرایرانی سرور در محیط تست) مطابق خط پایه‌ی مستند.

---

## 🔴 باگ ۱ — قفل‌شدن کامل برنامه بعد از لغو پنجره‌ی ورود (بحرانی)

**فایل:** `main.py`

**شرح:** در اولین اجرا، اگر کاربر پنجره‌ی ورود را رد می‌کرد (`Esc` یا بستن)،
`Controller.run()` → `self.quit()` → `app.quit()` فراخوانی می‌شد و بلافاصله بعدش
`main()` به `app.exec()` می‌رسید. در Qt، فراخوانی `quit()` **قبل از** `exec()` هیچ اثری
ندارد؛ نتیجه: رویدادنگار (event loop) تا ابد اجرا می‌شد در حالی که پنجره و تری هر دو
مخفی بودند — پروسه‌ی زامبی که فقط از Task Manager بسته می‌شد.

**رفع:**
- پرچم `_quit_requested` به `Controller` اضافه شد و `quit()` آن را ست می‌کند.
- `main()` قبل از `exec()` پرچم را چک می‌کند و مستقیم `sys.exit(0)` می‌زند.
- بعد از `exec()` هم خروج با `os._exit(code)` انجام می‌شود تا teardown پایتون هرگز
  روی تردِ شبکه‌ی در جریان کرش نکند (تنظیمات در همان لحظه ذخیره‌ی همگام شده‌اند).

**تأیید:** جریان واقعی `main()` با ورودِ لغوشده در زیرپروسه → خروج تمیز در `0.12s`.

---

## 🔴 باگ ۲ — کرش (SIGABRT) هنگام خروج/تغییر تنظیمات وسط درخواست شبکه (بحرانی)

**فایل:** `main.py`

**شرح:** `Poller.stop()` فقط `3` ثانیه برای اتمام ترد صبر می‌کرد؛ اگر درخواست شبکه
هنوز در جریان بود (تایم‌اوت‌ها تا ۲۰ ثانیه × ۲ تلاش + پروب کارت‌های شبکه)، مرجع
`self.poller = None` رها می‌شد و Garbage Collector شیء `QThread در حال اجرا` را نابود
می‌کرد → پیام `QThread: Destroyed while thread is still running` و **abort کل پروسه**.
سناریوهای تحریک: خروج از برنامه، تغییر بازه‌ی بررسی، خروج از حساب — هرکدام وسط یک
درخواست کند (مثلاً با VPN).

**رفع:** الگوی «بازنشسته» — پولرِ در جریان در `_retired_pollers` نگه داشته می‌شود و با
سیگنال `finished` آزاد و `deleteLater` می‌شود.

**تأیید:** درخواست ۶ ثانیه‌ای وسط کار + `gc.collect()` → بدون abort، تخریب پس از `finished`.

---

## 🟠 باگ ۳ — کرش در شروع برنامه با تنظیمات خراب

**فایل‌ها:** `storage.py`، `main_window.py`

**شرح:** `storage.load()` مقادیر فایل `settings.json` را بدون اعتبارسنجی برمی‌گرداند.
نمونه‌های کرش تأییدشده:
- `default_action: "bogus"` → `ValueError: 'bogus_value' is not in list` در ساخت `MainWindow`
- `poll_minutes: null` → `TypeError: int() argument must be...` در ساخت `Poller`
- `lead_minutes: "abc"` → `ValueError` در `spin_lead.setValue`

**رفع:** تابع `_sanitize()` در `storage.load()` — اعداد به بازه‌ی مجاز clamp می‌شوند،
`mode/default_action/theme` با whitelist چک می‌شوند و انواع لیست/رشته نرمال می‌شوند.
ایندکسِ `combo_action` هم به‌صورت امن (fallback به گزینه‌ی اول) ساخته می‌شود.

---

## 🟠 باگ ۴ — کرش در ورود اگر پاسخ «قبض‌ها» لیست باشد

**فایل:** `api.py`

**شرح:** `get_bills()` فرض می‌کرد `data` همیشه دیکشنری با کلید `bill_data` است؛ اگر
سرور خودِ `data` را لیست بدهد → `AttributeError: 'list' object has no attribute 'get'`
وسط جریان ورود (بعد از تایید کد OTP).

**رفع:** هر دو شکل پاسخ (لیست یا دیکشنری) پشتیبانی می‌شود.

---

## 🟡 باگ ۵ — رنگ‌های قدیمی بعد از تعویض تم روی ویجت‌های سفارشی

**فایل‌ها:** `theme.py`، `main.py`

**شرح:** سوییچ و حلقه‌ی شمارش رنگ را در `paintEvent` از پالت زنده می‌خوانند، اما هیچ
رویداد بازرسمی برایشان صادر نمی‌شد؛ `window.update()` فقط خودِ پنجره را دوباره
می‌کشد، نه ویجت‌های فرزند را. نتیجه: بعد از تعویض روشن/تیره، سوییچ‌ها و حلقه با رنگ
تم قبلی می‌ماندند تا اولین تعامل کاربر.

**رفع:** `theme.apply()` حالا بعد از اعمال QSS، همه‌ی ویجت‌های `QApplication.allWidgets()`
را `update()` می‌کند.

---

## 🟡 باگ ۶ — گزارش نادرست وضعیت و نتیجه‌ی عملیات

**فایل‌ها:** `api.py`، `power.py`

**شرح:**
1. `_probe()` قبل از موفقیت، `_route_state["source"]` را ست می‌کرد؛ اگر همه‌ی کارت‌ها
   رد می‌شدند، برچسب «کارت شبکه …» برای کارتی نمایش داده می‌شد که اصلاً جواب نداده بود.
2. `power.shutdown()` همیشه `True` برمی‌گرداند (حتی وقتی دستور `shutdown` با خطا خارج
   می‌شود) → پیام «اقدام خودکار ناموفق بود» هرگز صادر نمی‌شد و کاربر از شکست بی‌خبر می‌ماند.
   همین مشکل در فالبک `shutdown /h` هیبرنیت بود.

**رفع:** ثبت مسیر فعال فقط بعد از موفقیت پروب؛ بررسی `returncode` در `shutdown()` و
فالبک `hibernate_now()`.

---

## 📁 فایل‌های تغییرکرده

| فایل | تغییر |
|---|---|
| `main.py` | رفع hang لغو ورود، چرخه‌ی عمر امن پولر، خروج تمیز با `os._exit` |
| `storage.py` | اعتبارسنجی و نرمال‌سازی تنظیمات (`_sanitize`) |
| `main_window.py` | ایندکس امن کامبوی عمل پیش‌فرض |
| `api.py` | پذیرش پاسخ لیستی در `get_bills`، ثبت مسیر فقط بعد از موفقیت |
| `theme.py` | بازرسم همه‌ی ویجت‌ها پس از تعویض تم |
| `power.py` | بررسی returncode در خاموش کردن و فالبک هیبرنیت |

بقیه‌ی فایل‌ها (`util.py`, `widgets.py`, `login_dialog.py`, `warn_dialog.py`, `preview.py`,
`screenshot.py`) بررسی شدند و نیازی به تغییر نداشتند؛ منطق ایمنی «هیچ عملی برای خاموشیِ
گذشته» و «فرصت لغو ۳۰ ثانیه‌ای» سالم است و با تست بازتأیید شد.

---

## 🔁 نحوه‌ی اجرای تست‌ها

```bat
python test_smoke.py
```

تست‌های سناریو و رندر UI که در این دیباگ استفاده شد، به‌صورت اسکریپت جدا اجرا شدند
(شبیه‌سازی: لغو ورود، قطع وسط درخواست، تنظیمات خراب، پایان شمارش معکوس، تعویض تم).

---

## نسخه‌ی ۴ — دوزبانه، تک‌نمونه، سینک ویندوز، چند-قبضی

> این نسخه پاسخ به ۱۲ گزارش کاربر است: تک‌نمونه، لگِ بازه‌ی بررسی، سینک رنگ/تم
> ویندوز، نبود اسکرول‌بار، مسیر «قبض ثبت‌نشده»، ثبت چند خاموشی/چند قبض،
> فونت‌های ریز، انیمیشن‌های تند، باگ طراحی «قطعی بعدی»، نبود انگلیسی،
> نبود صفحات تنظیمات/راهنما/درباره و لحنِ بیش‌ازحد رسمی.

### آنچه اضافه/رفع شد

| # | گزارش کاربر | راه‌حل | فایل |
|---|---|---|---|
| ۱ | «دوبار کلیک روی exe = دو تا ناجی» | قفل تک‌نمونه با QLocalServer/QLocalSocket؛ کلیک دوباره فقط پنجره‌ی موجود را جلو می‌آورد + توست «ناجی همین‌حالا بازه» | main.py |
| ۲ | «انتخاب بازه‌ی بررسی لگ دارد» | دو ریشه: (الف) هر کلیکِ اسپین بلافاصله save+ری‌استارت ترد می‌کرد → دپ‌بان ۴۵۰ms؛ (ب) stop() پولر در رشته‌ی UI منتظر ترد می‌ماند → بدون wait + بازنشستگی با finished→deleteLater | main_window.py، main.py |
| ۳ | «رنگ UI با ویندوز سینک نشود/بشود» | wininfo.py: خواندن AccentColorMenu/ColorizationColor/AppsUseLightTheme از رجیستری + تایمر ۵ ثانیه‌ایِ کشف تغییر؛ apply_accent کل پالت، گرادیان هیرو، هاله‌ها و لکه‌ی اول شفق را هم‌فام می‌کند؛ کلید «هماهنگ با ویندوز» در تنظیمات | wininfo.py، theme.py، main.py |
| ۴ | «نوار کناری نداره، اسکرول نمی‌شه» | هر چهار صفحه در QScrollArea شیشه‌ای با اسکرول‌بار ۱۱ پیکسلیِ نمایان (قبلاً ۵ پیکسلیِ محو) | main_window.py، theme.py |
| ۵ | «شناسه قبضم توی برق‌من ثبت نیست» | به‌جای خطای بن‌بست، صفحه‌ی راهنمای چهارمرحله‌ای با دکمه‌ی «بررسی دوباره» و «برگرد به شماره» + پرسش/پاسخ کامل در صفحه‌ی راهنما | login_dialog.py، i18n.py |
| ۶ | «دوتا خاموشی خواستم ثبت کنم» | پایش چند-قبضی: مدیریت قبض‌ها (افزودن/حذف/فعال‌سازی) در تنظیمات، پولر همه‌ی قبض‌ها را در یک لیست تلفیق می‌کند، هر ردیف نشانِ قبض دارد، هیرو نشان قبضِ خاموشیِ بعدی را می‌گیرد | main.py، main_window.py، widgets.py |
| ۷ | «توضیحات ریز و ناخواناست» | تایپوگرافی یک‌درجه درشت‌تر: بدنه ۱۵px، muted ۱۴٫۵px، hint ۱۳٫۵px، دکمه‌ها ۱۴٫۵–۱۵٫۵px، ورودی‌ها ۱۵px، فونت پایه ۱۱pt | theme.py، widgets.py |
| ۸ | «انیمیشن‌ها تند است» | حرکت شفق ۰٫۷۸× آهسته‌تر، شیمر هیرو ۵٫۶ ثانیه، قرص سگمنت ۲۶۰ms با OutCubic؛ تیک انیمیشن فقط وقتی پنجره دیدنی است | widgets.py |
| ۹ | «باگ طراحی بخش قطعی بعدی» | چیدمان v4 هیرو: ستون متن از ستون تزئین جدا شد (مدار فقط نیمه‌ی دور)، خط آدرس با ارتفاع رزرو و بیرون‌نزدن از لبه، شمارش درشت نمایشی | widgets.py |
| ۱۰ | «زبان انگلیسی اضافه کن» | i18n.py با ~۱۵۰ رشته (فارسی/انگلیسی)؛ ارقام و RTL/LTR خودکار؛ تعویض زبان از تنظیمات با بازسازی تمیز پنجره | i18n.py، main.py |
| ۱۱ | «تنظیمات و درباره ما و … اضافه کن» | نوار کناری ۴ صفحه‌ای: خانه / تنظیمات / راهنما (۶ پرس‌وپاسخ) / درباره (داستان، طراحی، فونت‌ها، منبع داده) | main_window.py، widgets.py |
| ۱۲ | «متن‌ها خیلی رسمی و ضایع» | بازنویسی کامل لحن: «حالت خواب» به‌جای Sleep، «یه چرت سبک!»، «بی‌خیال»، «همینه!»، «خبر بده + اقدام» و… | i18n.py |

### 🔴 باگ تازه‌کشف‌شده در تست — ترتیب عملیات در تک‌نمونه (بحرانی)

**فایل:** main.py — `SingleInstance.try_become_primary`

در اولین پیاده‌سازی، `QLocalServer.removeServer` **قبل** از probe صدا زده می‌شد؛
یعنی نمونه‌ی دوم اول سوکتِ نمونه‌ی زنده را پاک می‌کرد، بعد چون probe وصل نمی‌شد،
خودش سرور می‌شد — نتیجه: دو ناجی با هم! ترتیب درست: اول probe، فقط در نبودِ
نمونه‌ی زنده، پاکسازیِ بقایای کرش و listen. تست اختصاصی
(`v4_feature_test.py`) این سناریو را با دو نمونه واقعی پوشش می‌دهد.

### 🔴 باگ تازه‌کشف‌شده — دکمه‌ی تمِ نوار کناری بی‌عملگر

`btn_theme` در NavRail ساخته می‌شد ولی به هیچ سیگنالی وصل نبود (همان خانواده‌ی
باگ «عملگر مرده»). حالا چرخه‌ی system→light→dark را اجرا و سگمنت «تم برنامه»
در تنظیمات را هم‌گام می‌کند.

### 🔴 باگ تازه‌کشف‌شده — نشان قبض روی کارت هیرو هرگز نمایش داده نمی‌شد

`fill_outage_list` روی کپی‌های خودش `_multi` می‌گذاشت، ولی `update_snapshot`
خاموشیِ خامِ لیست را به هیرو می‌داد که `_multi` نداشت → نشانِ قبضِ «قطعی بعدی»
در پایش چند-قبضی مخفی می‌ماند. حالا در update_snapshot هم ست می‌شود.

### 🧪 نتایج تست نسخه‌ی ۴

| تست | نتیجه |
|---|---|
| test_smoke.py (با run_smoke.py روی لینوکس) | ۱۹/۲۰ — تنها «مسیر شبکه‌ی زنده» طبق خط پایه‌ی مستند |
| scenario_test.py | ۷/۷ |
| scenario_test2.py | ۴/۴ |
| ui_interaction_test.py — کلیک واقعی QTest | ۲۴/۲۴ |
| v4_feature_test.py — تک‌نمونه/زبان/سینک/راهنما | ۱۵/۱۵ |
| رندر هر دو تم × هر دو زبان × همه‌ی صفحه‌ها | ۳۲ اسکرین‌شات OK (download/ui_v4) |

---

## نسخه‌ی ۴٫۱ — بازنگری طراحی به درخواست «دیزاینت مشکل داره»

بازخورد کاربر مبهم بود؛ به‌جای حدس، کل ۳۲ رندر (دو زبان × دو تم × هشت صحنه)
صحنه‌به‌صحنه بازبینی و ۸ ایراد بصریِ واقعی فهرست و رفع شد:

### ۱) برخورد متن شمارش معکوس با مدار تزئینی هیرو (باگ «خاموشی بعدی»)
متن فارسی «۱ روز و ۱۴ ساعت» با اندازه‌ی ثابت ۴۰px تقریباً تمام عرض کارت را
می‌گرفت و به مدار تزئینی می‌رسید. حالا قلم **خودتنظیم** است:
`HeroCard._fit_px` با `QFontMetrics` واقعی (سرِ هر اندازه‌ی پیکسلی، نه مقیاس
خطی — باگ اندازه‌گیری نسخه‌ی اول همین بازنگری) بزرگ‌ترین اندازه‌ی ۳۸→۲۱px را
که در فضای «کارت منهای حریم مدار» جا شود انتخاب می‌کند. مدار هم کوچک‌تر
(۰٫۸۶h→۰٫۷۰h)، کم‌رنگ‌تر و پایین‌تر رفت تا بافتِ پس‌زمینه شود، نه رقیب متن.

### ۲) سرریز شمارش معکوس از حلقه‌ی هشدار
«۳۸:۴۰:۰۵» در ۳۲px از قطر داخلی حلقه (۱۴۰px) بیرون می‌زد.
`RingWidget.set_text` حالا همان فیتِ واقعی را انجام می‌دهد (۳۰→۱۶px) و یک
**دیسک شیشه‌ای** داخل حلقه اضافه شد که بسترِ متن است و عدد را از حلقه جدا می‌کند.

### ۳) متنِ فارسی در UI انگلیسی به لبه‌ی مخالف می‌چسبید (باگ Qt)
کشف مهم این بازنگری: در Qt، `AlignLeft` روی متنِ RTL حتی در ویجتِ LTR متن را
**راست** می‌چسباند (رفتار leading-edge با جهتِ متن). نتیجه: عنوان قبضِ فارسی در
ردیف‌های تنظیماتِ انگلیسی سمت راست و شناسه‌اش سمت چپ می‌افتاد و ردیف «دو تکه»
دیده می‌شد. راه‌حل: `AlignLeft | AlignAbsolute` (ترازِ فیزیکی) در
`_phys_align()` — روی عنوان/شناسه‌ی قبض، آدرسِ کارت خاموشی، آدرس و خطِ زمانِ
هیرو و پنجره‌ی هشدار اعمال شد.

### ۴) خط تیره‌ی تمام‌قدِ اسکرول‌بار
هندل اسکرول‌بار `text3` توپر بود؛ هر جا محتوا کمی بلندتر از صفحه بود، یک نوار
خاکستریِ نزدیک به تمام‌قد کنار صفحه دیده می‌شد که شبیه خطای رندر بود.
هندل جدید نیمه‌شفاف (`scroll`/`scroll_hover` در پالت)، باریک‌تر (۱۰px) و با
کالای شفاف — حالا فقط «راهنمای موقعیت» است نه عنصر مزاحم.

### ۵) کپشن‌های کاشی آمار دوخطی و ناجور
«پیش‌آگاهی پیش از قطع» و «Happened today» در عرض کاشی می‌شکستند و دو کاشیِ
ردیف ناهم‌قد می‌شدند. متن‌ها کوتاه شد («پیش‌آگاهی قطع» / «Alert lead» /
«Check every»)، چیپ ۳۸→۳۴px، بسته‌شدن سطر کپشن خاموش؛ نسبت ستون‌ها هم
۵/۴→۴/۵ عوض شد تا کاشی‌ها نفس بکِشند (شمارش هیرو خودتنظیم است و جبران می‌کند).

### ۶) خلأ بزرگ زیر لیست خاموشی‌ها
کارت خاموشی‌ها تا پایین کشیده می‌شد ولی لیست سه‌چهار آیتمی فقط بالایش را پر
می‌کرد. پایین کارت یک ردیف **خلاصه‌ی زنده** اضافه شد: نقطه‌ی رنگی + «۲ در راهه»،
نقطه‌ی سبز + «۱ مورد امروز»، و سمت دیگر «پایش ۳ قبض» — فضای خالی حالا
اطلاعاتِ مفید می‌دهد.

### ۷) دکمه‌ی اصلی تمام‌عرضِ سنگین
«به‌روزرسانی فوری» ~۶۰۰px گرادیان می‌کشید. حالا عرضش به ۳۸۰px مهار شده و
وسطِ ردیف کنار «خروج از حساب» می‌نشیند.

### ۸) صفحه‌ی درباره‌ی خلوت
با گرید ۲×۲ «HIGHLIGHTS» (چهار ویژگی کلیدی با چیپ آیکونی) پر شد.

### اصلاحات مکمل
- placeholder و متنِ فیلدهای ورود در فارسی حالا **راست‌چین** است (قبلاً به
  لبه‌ی چپ می‌چسبید و با لیبل‌های راست‌چین ناهماهنگ بود).
- عنوان قبض در خطِ سربرگِ انگلیسی با جداکننده‌ی ایزولهٔ bidi (LRI…PDI) محصور
  شد تا ترتیب کلمات به‌هم نریزد.
- NavRail یک خط مرزی ظریف سمتِ محتوا گرفت — جدایی فضایی بدون سنگینی.
- نسخه ۴٫۰٫۰→۴٫۱٫۰.

### 🧪 نتایج تست نسخه‌ی ۴٫۱

| تست | نتیجه |
|---|---|
| test_smoke.py (با run_smoke.py) | ۱۹/۲۰ — تنها «مسیر شبکه‌ی زنده» طبق خط پایه‌ی مستند |
| scenario_test.py / scenario_test2.py | ۷/۷ و ۴/۴ |
| ui_interaction_test.py — کلیک واقعی QTest | ۲۴/۲۴ |
| v4_feature_test.py | ۱۵/۱۵ |
| رندر مجدد ۳۲ اسکرین‌شات (دو زبان × دو تم × هشت صحنه) | OK — download/ui_v4 |


---

## نسخه‌ی ۴٫۲ — نشان «درباره ما» و پاک‌سازی کارت هیرو

### درخواست کاربر
1. «چنین لوگویی برا درباره ما میخوام» — تصویر مرجع: آیکون دو چهره + کلمه‌ی ABOUT US.
2. «بکگراند کادر قطعی بعدی که رنگش بنفشه اون دایره ها توش حذف بشن» — حلقه‌های مدارِ تزئینی روی کارت بنفش.
3. (دنباله‌ی همین درخواست) «این لوگو دایره ای هم با صاعقه عوض بشه» — نشانِ دایره‌ایِ مدار در حالتِ خالیِ لیستِ خاموشی‌ها با صاعقه عوض شد.

### تغییرات
- **آیکون جدید `users`** در icons.py: دو نیم‌تنه (نفر جلو کامل + نفر پشتِ کوچک‌تر) با زبان دودوتونه‌ی برنامه — پارس و رندر تأیید شد (پیکسل غیرشفاف در هر دو زبان).
- **ویجت `AboutUsLogo`** در widgets.py: قرص گرادیانی ۷۰px با گلیف دو چهره + واژه‌نگار زیرش («درباره ما» / «ABOUT US» با فونت نمایشی). الگوی چیدمان از تصویر مرجع کاربر.
  - نکته‌ی تایپوگرافی: `letter-spacing` فقط برای LTR اعمال می‌شود — در فارسی این ویژگی اتصال حروف را می‌شکند (در رندر اول دیده و رفع شد).
- **NavRail**: آیکون صفحه‌ی «درباره» از spark به users تغییر کرد تا با نشان صفحه هم‌هویت باشد.
- **EmptyState**: مرکز ترکیبِ حالت خالی از مدارِ دایره‌ای به صاعقه‌ی ۸۴px ناجی تغییر کرد؛ دو جرقه‌ی تزئینی ماندند.
- **HeroCard**: حلقه‌های مدار (SVG orbit) از paintEvent حذف شد؛ هاله‌های نرم نور (blob) که «دایره» دیده نمی‌شوند ماندند. با حذف مدار، حریمِ عرضیِ آن هم آزاد شد و شمارش معکوس حالا می‌تواند از تمام عرض کارت استفاده کند (در تست: قلم ۳۸px در عرض ۵۶۰ به‌جای سقف قبلی).
- `LogoChip` پارامتر اختیاری `glyph` گرفت (پیش‌فرض bolt — لوگوی سربرگ دست‌نخورده).
- نسخه ۴٫۱٫۰ → ۴٫۲٫۰ (main_window و setup.iss).

### درخواست دنباله‌دار: نصاب ویزاردی با انتخاب درایو
- کاربر خواست نصاب مثل MSI Afterburner چندصفحه‌ای باشد و کاربر درایو/پوشه‌ی نصب را انتخاب کند.
- setup.iss: سه صفحه‌ی قبلاً خاموش، روشن شد — DisableWelcomePage=no (خوش‌آمد)، DisableDirPage=no (انتخاب درایو/پوشه)، DisableProgramGroupPage=no (منوی استارت).
- متن‌های تسک/اجرای پس از نصب به پیام‌های استاندارد cm: منتقل شد تا با زبان انتخابی نصاب (فا/EN) خودکار ترجمه شوند؛ میان‌بر «حذف» هم CustomMessages دوزبانه گرفت.
- UninstallDisplayIcon و متادیتای نسخه به فایل نصاب اضافه شد؛ PrivilegesRequirements=lowest ماند (بدون نیاز به ادمین؛ پیش‌فرض = پوشه‌ی برنامه‌های کاربر).
- build_setup.bat (جدید): ساخت نصاب با یک دوبار-کلیک — پیدا کردن ISCC در Program Files، چک بودن dist\Naji.exe، کامپایل و اعلام مسیر Output\NajiSetup.exe.
- README بخش «ساخت نصاب نصب» سه‌قدمی نوشته شد.

### نتایج تست نسخه‌ی ۴٫۲
| تست | نتیجه |
|---|---|
| check_v42.py (رندر آیکون users، واژه‌نگار، نبود orbit در HeroCard، آزادشدن عرض شمارش) | OK |
| test_smoke.py | ۱۹/۲۰ — تنها «مسیر شبکه‌ی زنده» طبق خط پایه‌ی مستند |
| scenario_test.py / scenario_test2.py | ۷/۷ و ۴/۴ |
| ui_interaction_test.py | ۲۴/۲۴ |
| v4_feature_test.py | ۱۵/۱۵ |
| رندر مجدد ۳۲ اسکرین‌شات | OK — download/ui_v4 |

### رفع اشکال ۴٫۲٫۱: «پنجره‌ی سیاه یک لحظه باز می‌شود و غیب می‌شود» (ساخت روی ویندوز)
**گزارش کاربر:** هنگام اجرای فایل‌های ساخت، پنجره‌ای لحظه‌ای باز می‌شد و بی‌هیچ پیامی بسته می‌شد.

**ریشه‌یابی:** فایل‌های bat قبلی دو نقص جدی داشتند:
1. **انتهای خط LF (یونیکسی)** به‌جای CRLF — cmd در ویندوز فایل bat را فقط با CRLF درست پارس می‌کند؛ با LF، خطوط پیوسته‌ی `^` (دستور ۱۴خطی PyInstaller) و بلوک‌های `(...)` خراب می‌شوند، cmd با خطای syntax کل فایل را همان اول رها می‌کند و **قبل از رسیدن به `pause`** خارج می‌شود — دقیقاً همان «پنجره‌ی لحظه‌ای».
2. **متن فارسی UTF-8 داخل bat** — روی بسته‌های زبانی مختلف ویندوز (کدپیج‌های غیر UTF-8) خطر پارس‌خطا و mojibake داشت؛ ضمناً فونت کنسول فارسی را با شکل‌دهی درست نشان نمی‌دهد.

**اصلاح:** هر چهار فایل `build_exe.bat`، `build_setup.bat`، `run.bat` (و جدید: `check_env.bat`) بازنویسی شدند:
- **ASCII خالص + CRLF** (راستی‌آزمایی با `file`: «ASCII text, with CRLF line terminators»؛ بدون BOM) — پیام‌های کنسول انگلیسی ساده، متن فارسیِ کامل در README.
- **چک خطا در هر مرحله + `pause` در تمام مسیرهای خروج** — دیگر هیچ مسیری پنجره را بدون مکث نمی‌بندد؛ متن خطا در پنجره می‌ماند.
- `build_exe.bat`: یافتن پایتون (ابتدا `python`، سپس لانچر `py -3`)، نصب خودکار `pyinstaller PySide6 requests jdatetime` (قبلاً فقط pyinstaller — نبودِ PySide6 خطای «No module named» می‌داد)، تأیید وجود `dist\Naji.exe` در پایان.
- `check_env.bat` (جدید): «پزشک محیط» — وضعیت Python / pip / PyInstaller / ISCC را با مسیر دقیق نشان می‌دهد؛ اگر باز هم خطایی رخ دهد، خروجی همین فایل اسکرین‌شات می‌شود.
- `build_setup.bat`: چک `dist\Naji.exe`، یافتن ISCC در هر دو Program Files، کامپایل، زیپ Naji-Setup.zip، همه با پیام خطای قابل‌خواندن.
- README: بخش «عیب‌یابی: پنجره‌ی سیاه…» با سه علت رایج (PATH پایتون / اجرا از داخل زیپ / نسخه‌ی قدیمی bat).

## v4.2.1 — «دکمه‌ی بفرست کد رو کار نمی‌کنه» + «فونت‌ها باگ دارند» (گزارش کاربر)

**علامت:** کاربر گزارش داد دکمه‌ی «بفرست کد رو» در پنجره‌ی ورود هیچ عکس‌العملی ندارد و «فونت‌ها باگ دارند».

### باگ ۱ — دکمه‌ی ارسال کد (و تایید کد و بررسی دوباره) عملاً مرده بود

**ریشه‌یابی:** امضای تابع `_run` در `login_dialog.py` چنین بود:

```python
def _run(self, fn, *args, on_done):   # on_done بعد از *args = فقط‌کیوردی
```

ولی هر سه فراخوانی، کال‌بک را **موقعیتی** می‌دادند: `_run(api.send_otp, mobile, self._on_send_done)` — یعنی کال‌بک داخل `*args` می‌نشست و پایتون با `TypeError: missing 1 required keyword-only argument: 'on_done'` وسط اسلاتِ کلیک می‌مرد. در اجرای از سورس، traceback فقط در stderr است؛ در **exe پنجره‌ای هیچ خروجی‌ای نیست** — نتیجه: کلیک بدون هیچ واکنشی. بدتر اینکه `_run(self._refetch_bills, self._on_refetch_done)` کال‌بک را به‌جای آرگومان `token` می‌فرستاد.

**تست بازتولید:** `scripts/render_login.py` — کلیک واقعی روی دکمه با موک شبکه؛ پیش از اصلاح: `TypeError` در stderr + ماندن روی صفحه ۱؛ پس از اصلاح: عبور به صفحه‌ی کد / نمایش خطا.

**اصلاح:**
- امضای `_run(self, fn, on_done, *args)` و اصلاح هر سه فراخوانی (`_refetch_bills` حالا `self.token` را می‌گیرد).
- `_guard()`: هر استثنای داخل اسلات‌های کلیک به پیام قابل‌دیدن در نوار پایین دیالوگ تبدیل می‌شود — دیگر هیچ باگی «دکمه‌ی مرده» نمی‌سازد.
- راهنمای «هنوز داریم به برق‌من وصل می‌شیم…» بعد از ۶ ثانیه انتظار در نوار پیام (رنگ خنثی) — انتظار طولانیِ شبکه دیگر شبیه بن‌بست نیست.

### باگ ۲ — متن فیلدها و شمارش معکوس به لبه‌ی چپ می‌چسبید («فونت‌ها باگ دارند»)

**ریشه‌یابی:** در چیدمان RTL، فلگ‌های `Qt.AlignRight`/`AlignLeft` **آینه می‌شوند** (مگر با `Qt.AlignAbsolute`). probe (`scripts/probe_align.py`): در RTL، `AlignRight` نشانگرِ موقعیت صفر را در x=24 (لبه‌ی چپ!) می‌گذارد؛ `AlignAbsolute|AlignRight` → x=261 (راستِ واقعی). یعنی:
- placeholder و شماره‌ی موبایل/کد در هر سه فیلد ورود به لبه‌ی چپ می‌چسبیدند (در حالی که لیبل‌ها راست‌چین‌اند) — حس «فونت/چیدمان شکسته».
- شمارش معکوس درشتِ کارت هیرو (`_start_align`) به چپ می‌چسبید و از ردیف‌های راست‌چین زیر خودش جدا می‌افتاد.

**کاوش فونت (خنثی شد):** جدول نام TTF‌ها با fontTools بازرسی شد — هر ۵ فایل Estedad «Estedad/Regular» و هر دو فایل Space Grotesk «Light» ثبت شده‌اند؛ ولی `QFontInfo` ثابت کرد مچ‌وزنِ Qt از OS/2 usWeightClass درست کار می‌کند (w900→Black واقعی)؛ پس فایل‌ها دست‌نخورده ماندند (احترام به OFL) و مشکل واقعی، ترازِ آینه‌شده بود نه خود قلم.

**اصلاح:** `_field_align()` در `login_dialog.py` و `_start_align()` در `widgets.py` حالا `AlignAbsolute` برمی‌گردانند (الگوی درستِ موجودِ `_phys_align`).

**رگرسیون:** سموک ۱۹/۲۰ (تنها خطای پایه‌ی شبکه‌ی زنده: IP خارج از ایران بلاک است)، سناریو ۷/۷، سناریو-۲ ۴/۴، تعامل UI سبز، v4-feature ۱۵/۱۵، V42 سبز، کلیکِ ورود ۵/۵، رندر ۳۲ صحنه + ۹ صحنه‌ی ورود تازه.

## v4.3.0 — شش گزارش کاربر: قفل «در حال بررسی»، سینک تم/رنگ ویندوز، فونت‌های ناهمگون، آیکون خانه، حذف FAQ دوبار‌کلیک، نام Naji در نصاب

**علامت‌ها (گزارش کاربر):** ۱) چیپ «در حال بررسی» قفل می‌زند ۲) تم و رنگ‌آمیزی با ویندوز سینک نمی‌شود ۳) در یک جمله یک کلمه پررنگ و یکی نازک/پیکسلی ۴) آیکون خانه شبیه حلقه‌ی بارگذاری است ۵) کارت «دوبار کلیک» در راهنما اضافی است ۶) اسم برنامه در نصاب و آیکون دسکتاپ باید Naji باشد.

### باگ ۱ — قفل چیپ «در حال بررسی» (بحرانی — ریشه‌یابی جدید)

**ریشه:** قرارداد توابع ناهم‌خوان بود. `main.py` در اسلاتِ `_on_snapshot` می‌نوشت:

```python
kind, detail = api.active_source()   # unpack دوتایی
```

ولی `api.active_source()` یک **رشته‌ی ساده** («مسیر پیش‌فرض») برمی‌گرداند، نه تاپل → `ValueError: too many values to unpack` وسط اسلات. چون این ValueError **قبل از** `set_connection` رخ می‌داد، چیپ هرگز از «در حال بررسی…» بیرون نمی‌آمد (حتی با داده‌ی سالم!) و هشدارها هم مسلح نمی‌شدند. در exe بدون کنسول، کاملاً خاموش بود.

**اصلاح:**
- `api.active_source()` حالا `("default", None)` یا `("nic", ip)` برمی‌گرداند — قرارداد درست.
- `_on_snapshot` بازچینی شد: اول چیپ اتصال، بعد رندر لیست/هیرو، بعد منطق هشدار — هر بخش در try مستقل؛ هیچ بخشی نمی‌تواند چیپ را گروگان بگیرد.
- **نگهبان ۹۰ ثانیه‌ای:** هر «بررسی الان»/چرخه‌ی جدید یک تایمر تک‌شات می‌سازد؛ اگر هیچ پاسخی (موفق/خطا) نرسد، چیپ به پیام «بررسی طول کشید — اینترنت را چک کن…» می‌رود — دیگر هیچ‌وقت قفل نیست.
- `_check_now` اگر پولر مرده باشد آن را دوباره راه می‌اندازد؛ `_start_poller` هم بلافاصله چیپ را «در حال بررسی» می‌کند (بازخورد صادقانه در شروع).

### باگ ۲ — تم با ویندوز سینک نمی‌شد

**ریشه:** در `_on_theme_mode_changed`، برای حالت «system» پارامتر `reset_theme=False` می‌شد؛ یعنی انتخابِ «هماهنگ با ویندوز» (یا چرخش دکمه‌ی تم نوار کناری روی system) تمِ قبلی را نگه می‌داشت — ویندوزِ روشن، اپِ تیره می‌ماند.

**اصلاح:** تغییر حالت تم همیشه `reset_theme=True`؛ در حالت system، تم از `wininfo.apps_dark()` مشتق می‌شود. پایش ۵ ثانیه‌ای تغییر ظاهر ویندوز سر جایش است.

### باگ ۳ — رنگ‌آمیزی با ویندوز اثر محسوس نداشت

**اصلاح:** آستانه‌ی اشباعِ اکسنت از ۲۴ به ۱۲ نرم شد (اکسنت‌های کم‌رنگ هم اعمال می‌شوند؛ فقط خاکستریِ کامل که فام ندارد رد می‌شود). در تم روشن، اکسنت‌های پاستلی برای خواناییِ متنِ سفید مهار می‌شوند (s≥۰٫۵، v≤۰٫۸) و اکسنت سالم **بدون هیچ تبدیلی** پاس می‌شود تا رنگ ویندوز دقیق بماند.

### باگ ۴ — فونت‌ها: یک کلمه پررنگ، یکی نازک/پیکسلی (این بار ریشه‌ی واقعی)

**کاوش جدید با fontTools** (دفعه‌ی قبل «فایل‌ها سالم‌اند» نتیجه‌اش از QFontInfo لینوکس بود؛ ویندوز فرق می‌کند):

```
Estedad-Black.ttf   ID1='Estedad'  ID2='Regular'  ID16/17 = ندارد!
Estedad-Bold.ttf    ID1='Estedad'  ID2='Regular'  ← پنج فایل کاملاً یکسان در نام!
SpaceGrotesk-*.ttf  ID1='Space Grotesk Light'  ← هر دو فایل اشتباهی Light!
```

پنج فایل استedad همه «Estedad Regular» بودند و فقط `OS/2.usWeightClass` فرق داشت. تطبیق‌دهنده‌ی فونت ویندوز که به نام سبک اعتماد می‌کند، از میان پنج قلمِ هم‌نام دل‌بخواه انتخاب می‌کرد → تیترها گاهی Medium گاهی Black؛ در کنار hinting کاملِ GDI روی فونتِ بی‌hinting فارسی، ساق‌ها ناهمگون تیز می‌شدند (پیکسلی/یکی پُر یکی نازک).

**اصلاح (`scripts/fix_fonts.py`، اجرا شده روی فایل‌های باندل):**
- طرح نام استاندارد WWS (مثل فایل‌های رسمی Vazirmatn): هر وزن `ID1="Estedad <Weight>"` + `ID16="Estedad"` + `ID17="<Weight>"`؛ Bold با بیت‌های bold درست در fsSelection/macStyle.
- Space Grotesk: نام‌های غلطِ Light اصلاح شد (Medium واقعاً Medium، Bold با بیت bold).
- `theme.display_family(weight)`: QSS و قلم‌های درون‌خطی حالا **خانواده‌ی تک‌فایلیِ دقیق** می‌گیرند (`h1→"Estedad Black"`، `h2/statValue/stepChip→"Estedad ExtraBold"`…) — تطبیق وزنِ مبهم به‌کلی از دور خارج شد.
- `app_font()`: `PreferNoHinting` — رندر نرم و همگن روی ویندوز (رفع «پیکسل پیکسل»).

**تأیید:** QFontDatabase بعد از load_fonts: `Estedad styles: Medium/SemiBold/Bold/ExtraBold/Black` + `Estedad@900 → family='Estedad Black' weight=900`؛ h1 رندرشده `('Estedad Black', 900)`.

### بقیه‌ی موارد
- **آیکون خانه:** SVG خانه (سقف + بدنه + در) به icons.py اضافه شد؛ NavRail از `orbit` (دایره‌ی خط‌چین که شبیه اسپینر بود) به `home` رفت.
- **FAQ «دوبار کلیک»:** از صفحه‌ی راهنما حذف شد (۵ سوال اول ماندند).
- **نام نصاب:** `MyAppName` از «ناجی» به `Naji` تغییر کرد → عنوان نصاب، آیکون دسکتاپ، گروه استارت‌منو و ورودی حذف برنامه همه «Naji» هستند (AppId ثابت ماند → ارتقا روی نصب قبلی سوار می‌شود).

**رگرسیون:** سموک ۱۹/۲۰ (خطای پایه‌ی شبکه‌ی زنده)، سناریو ۷/۷، سناریو-۲ ۴/۴، تعامل سبز، v4-feature ۱۵/۱۵ (بعد از سازگارسازی تست F3 با پاس‌ترُ اکسنت سالم)، V42 سبز، رندر ۳۲ صحنه + ۶ صحنه‌ی متمرکز v4.3.

---

## v4.3.1 — «اعلان ۱۲ دقیقه قبل نیومد» + دکمه‌ی ثبت

### باگ ۱ — یادآورِ تنظیم‌شده اعلان نمی‌داد (ریشه‌ی واقعی)

**فایل:** `storage.py` / `main.py`

`warned` (لیست dedupe هشدارها) روی دیسک ذخیره می‌شد و **هرگز پاک نمی‌شد**. کلید هشدار (`warn:|قبض|تاریخ|ساعت|آدرس`) یک‌بار مصرف بود — اگر همان خاموشی حتی یک‌بار هشدار خورده بود (اجرای قبلی برنامه، پیش‌آگاهی قبلی، یا توستی که دیده نشد)، تغییرِ «چند دقیقه قبل» به ۱۲ هم دیگر **هرگز** هشدار تازه شلیک نمی‌کرد؛ `_check_warnings` بی‌صدا `continue` می‌کرد و هیچ ردپایی هم برای کاربر نمی‌ماند که آیا هشدار رفته یا نه.

**اصلاح:**
- `storage.clear_warned()` جدید — در `Controller.run()` صدا زده می‌شود: dedupe فقط در **همین اجرا** معتبر است؛ هشدارِ تکراری همیشه بهتر از سکوتِ بی‌صدا است.
- `_on_settings_changed`: تغییرِ `lead_minutes` (مقایسه با `_armed_lead`) → پاک‌سازی dedupe → خاموشی‌های پیش‌رو با پیش‌آگاهیِ تازه از نو مسلح می‌شوند.
- ردپای هشدار: `storage.set_last_warn()` بعد از هر شلیک (ساعت + خلاصه‌ی خاموشی) → کارت تنظیمات «آخرین هشدار: ساعت …» را نشان می‌دهد؛ تفکیکِ «شلیک نشد» از «ویندوز توست را نشان نداد» (فوکوس‌اسست/تنظیمات نوتیفیکیشن).
- توستِ هشدار در حالت «فقط خبر بده» ۱۲→۱۸ ثانیه شد.

### ویژگی ۲ — دکمه‌ی «ثبت تنظیمات هشدار» + یقینِ «ست شده»

**فایل:** `main_window.py` / `i18n.py` (درخواست مستقیم کاربر)

تنظیمات قبلاً ۴۵۰ms بعد از آخرین تغییر بی‌صدا ذخیره می‌شد و هیچ بازخوردی نبود. حالا در کارت هشدار:
- **دکمه‌ی «ثبت تنظیمات هشدار»** (primary): ذخیره‌ی فوری + متن تأیید «ثبت شد — هشدار ۱۲ دقیقه قبل از قطعی فعاله»؛ دکمه ۴ ثانیه «ثبت شد» می‌ماند و برمی‌گردد.
- **خط وضعیت مسلح** همیشه مرئی: «یادآور فعاله: ۱۲ دقیقه قبل از شروع خاموشی خبرت می‌کنیم» — با تغییر اسپین زنده به‌روز می‌شود.
- **خط ردپای هشدار**: «آخرین هشدار: ساعت ۲۱:۳۸ برای خاموشی …» یا «هنوز هشداری شلیک نشده…».
- راهنمای q4/a4 با توضیح دکمه‌ی ثبت و ردپای هشدار به‌روز شد.

**تأیید:** `scripts/warn_flow_test.py` — ۱۶/۱۶ سبز (dedupe پاک‌سازی، re-arm با تغییر lead، ماندگاری dedupe بدون تغییر lead، ذخیره‌ی فوری ۱۴ دقیقه، تأیید دیدنی، بازنشانی بازخورد، ردپا در هر دو زبان، توست ۱۸ثانیه‌ای). رندر: `download/ui_v431/` (روشن/تیره/انگلیسی — دکمه، تأیید، وضعیت مسلح و ردپا همه مرئی).

## v4.4.0 — «نیازی به بازه‌ی زمانی نیست» + «رنگ‌ها با ویندوز مچ نمی‌شن» + واژه‌ی «کارت»

### ساده‌سازی ۱ — حذف هر دو «بازه‌ی زمانی» از تنظیمات (درخواست صریح کاربر)

**استدلال کاربر درست بود:** ناجی هر ۲۰ ثانیه زمان خاموشی‌ها را با ساعت سیستم می‌سنجد؛ پس چه چیزی در «چند دقیقه قبل» و «فاصله‌ی چک کردن برق‌من» برای تنظیم کردن هست؟ فقط دو عدد برای گم‌شدن و شک («گزینه کار نمی‌کنه»).

**اصلاح:**
- اسپین «چند دقیقه قبل» و اسپین «فاصله‌ی پایش» از کارت هشدار حذف شدند. کارت حالا فقط سه چیز دارد: حالت هشدار (سگمنت)، اقدام پیش‌فرض (کامبو) و دکمه‌ی «ثبت تنظیمات هشدار».
- پیش‌آگاهی به **ثابت داخلی ۱۰ دقیقه** تبدیل شد (`lead_minutes` در DEFAULTS=10؛ دیگر هیچ UI ای عوضش نمی‌کند). هشدار خودکار: به‌محض ورود خاموشی به پنجره‌ی ۱۰ دقیقه‌ای، اعلان/پنجره شلیک می‌شود.
- `_on_settings_changed` ساده شد: فقط پولرِ مرده را دوباره راه می‌اندازد (منطق re-arm با `_armed_lead` حذف شد — عدد قابل تغییر دیگر وجود ندارد).
- وضعیت مسلح: «یادآور فعاله: حدود ۱۰ دقیقه قبل از شروع هر خاموشی خبرت می‌کنیم» + تأیید ثبت: «ثبت شد — هشدار قطعی فعاله».
- کاشی داشبورد «هشدار پیش از قطع» همان ۱۰ دقیقه ثابت را نشان می‌دهد.
- راهنمای q4/a4 و about.f2s با دنیای جدید همگام شدند.

### باگ ۲ — «رنگ‌ها با ویندوز مچ نمی‌شن» (ریشه‌ی واقعی)

**فایل:** `theme.py` / `wininfo.py`

دو لایه رنگ را خراب می‌کرد:
1. **تم روشن:** هر اکسنتی با روشنایی بالای ۰٫۸ به‌زور تیره می‌شد (`v2 = min(vv, 0.80)`) — زرد ویندوز (#FFB900) در برنامه قهوه‌ای- خردلی دیده می‌شد!
2. **تم تیره:** اشباع به باند ۰٫۴۲–۰٫۶۲ برده و روشنایی ۰٫۹۴ می‌شد — آبی ویندوز (#0078D4) یک آبیِ پاستلیِ متفاوت می‌شد و قرمز هم به صورتیِ کم‌رنگ بدل می‌شد. کاربر آبی می‌گذاشت، برنامه رنگِ دیگری نشان می‌داد.
3. گیت `wininfo` اکسنت‌های خاکستری/بی‌فام را هم رد می‌کرد و به رنگ امضای ناجی برمی‌گشت.

**اصلاح (پاسِ وفادار):**
- `apply_accent` حالا رنگ اکسنت ویندوز را **عیناً** روی هر دو تم می‌نشاند (آبی همان آبی، قرمز همان قرمز، زرد همان زرد) — فام، اشباع و روشنایی دست‌نخورده.
- خوانایی با **رنگ متنِ تطبیقی** حل شد: `on_accent` بر اساس روشنایی ادراکی YIQ انتخاب می‌شود — روی اکسنت‌های روشن (زرد/نارنجی روشن) متنِ دکمه‌ها تیره می‌شود (همان کاری که خود ویندوز می‌کند)، روی بقیه سفید می‌ماند.
- در تم تیره فقط اکسنت‌های خیلی تیره (سرمه‌ای و مانند آن، v<0.45) کمی روشن می‌شوند تا روی شیشه دیده شوند؛ بقیه عیناً پاس می‌شوند.
- گیت `wininfo._sat_ok` فقط سیاهِ نزدیک-خالص را رد می‌کند (mx<32)؛ خاکستری و سفید هم اکنون پاس می‌شوند. منبع سوم خواندن اکسنت هم اضافه شد (`AccentColor` — ARGB) کنار `AccentColorMenu` (COLORREF) و `ColorizationColor` (DWM).
- تغییر رنگ ویندوز حین کار برنامه همان‌طور که قبلاً بود ظرف ≤۵ ثانیه با تایمر `win_watch` اعمال می‌شود.
- متن چیپ‌ها (`_tint_chips`) هم روی اکسنت‌های روشن تیره می‌شود.

### اصلاح ۳ — واژه‌ی «کارت» (گزارش کاربر)

در نوار وضعیت، مسیر اتصال از طریق IP مشخص «کارت {nic}» نمایش داده می‌شد (ترجمه‌ی تحت‌اللفظی NIC) — برای کاربر واژه‌ی غلط و گیج‌کننده‌ای بود. به **«آی‌پی {nic}» / "IP {nic}"** اصلاح شد. واژه‌ی «کارت» از help.a4 هم رفت («همون بخش هشدار»).

**تأیید:**
- `scripts/accent_sync_test.py` جدید — ۲۰/۲۰: آبی/قرمز عیناً پاس، زرد با متن تیره، سرمه‌ای فقط در تم تیره روشن می‌شود، گیت جدید، ریست به رنگ امضا.
- `scripts/warn_flow_test.py` — ۱۷/۱۷ (اسپین‌ها حذف‌اند، وضعیت مسلح ثابت، پولرِ مرده راه می‌افتد).
- `scripts/ui_interaction_test.py` — سبز؛ v4_feature ۱۵/۱۵؛ سناریو ۷/۷ + ۴/۴؛ سموک ۱۹/۲۰ (همان خط پایه‌ی شبکه‌ی محیط).
- رندر: `download/ui_v440/` — ۱۵ صحنه شامل سه اکسنت ویندوز (آبی/قرمز/زرد) در هر دو تم.

---

## v4.4.1 — زمان هشدار به دستِ کاربر + نوار ناوبری فقط‌آیکونی

### گزارش کاربر

1. «میخوام زمان اون اعلان رو خود کاربر تنظیم کنه» — در ۴٫۴ اسپینِ «چند دقیقه قبل» به‌اشتباه حذف شده بود؛ کاربر زمانِ اعلان را می‌خواهد **خودش** تعیین کند.
2. «وقتی هوم یا هلپ رو انتخاب میکنی زیرش میاد رو حذف کن و فقط حالت رنگی که دورشه و تغییر رنگش کافیه» — برچسبِ متنی زیر آیکون‌های ناوبری باید برود؛ فقط قرص رنگی دور آیکون و تغییر رنگ آیکون کافی است.

### اصلاح ۱ — اسپین «چند دقیقه قبل از قطعی خبر بدهیم؟» (۱ تا ۱۲۰)

- `main_window._page_settings`: اسپین شیشه‌ای `spin_lead` به کارت هشدار برگشت (پیش‌فرض از تنظیمات ذخیره‌شده؛ پسوند «دقیقه»؛ وسط‌چین در RTL)؛ `valueChanged` به `_queue_settings` وصل است — عدد با همان دکمه‌ی «ثبت» و تأیید دیدنی ذخیره می‌شود.
- `_queue_settings` مقدار را در `settings["lead_minutes"]` می‌نویسد؛ منطق هشدار (`main._check_warnings`) از قبل همان را زنده می‌خواند — پس اعلان دقیقاً در عددِ ثبت‌شده شلیک می‌شود. کاشیِ «هشدار پیش از قطع» داشبورد و «یادآور فعاله: …» هم لحظه‌ای تازه می‌شوند.
- «فاصله‌ی چک کردن برق‌من» همچنان حذف است — چرخ پایش ۲۰ ثانیه‌ای داخلی است و نیازی به تنظیم ندارد (همان استدلال درستِ خود کاربر).
- متن راهنمای زیر اسپین این را صریح می‌گوید تا ابهام «بازه‌ی زمانی» دیگر پیش نیاید.

### اصلاح ۲ — NavButton فقط‌آیکونی

- برچسب متنی زیر آیکون و نقطه‌ی اکسنتِ زیر برچسب حذف شد؛ دکمه ۷۶×۶۲ → ۷۶×۴۶ و آیکون وسط دکمه.
- نشانه‌ی انتخاب: قرص شیشه‌ای با **مرز اکسنت** (آلفای ۱۵۰) + خودِ آیکون با رنگ اکسنت — دقیقاً «حالت رنگی دورش و تغییر رنگش».
- نام صفحه به‌صورت tooltip در دسترس ماند (با نگه‌داشتن ماوس)؛ ترجمه‌ی زنده هم tooltip را تازه می‌کند.

**تأیید:**
- `scripts/warn_flow_test.py` — بازنویسی بخش اسپین‌ها: `spin_lead` هست (۱..۱۲۰)، `spin_poll` نیست، متن مسلح عددِ کاربر را نشان می‌دهد.
- `scripts/ui_interaction_test.py` — بخش ۲-۳ بازنویسی: تغییر واقعی اسپین (۲۵) به `settings` و متن مسلح می‌رسد؛ بعد برگشت به مقدار ذخیره‌شده.
- `scripts/v4_feature_test.py` — ۱۵/۱۵ (بدون تغییر — `_text` هنوز برای tooltip/تست هست)؛ `accent_sync` ۲۰/۲۰؛ سناریوها سبز؛ سموک فقط خط پایه‌ی شبکه‌ی محیط.
- رندر: `download/ui_v441/` — ۱۸ صحنه: داشبورد/تنظیمات(+ثبت)/راهنما/درباره در دو تم + EN + سه اکسنت ویندوز با نوار جدید.

---

## v4.4.2 — برگشت متنِ زیر آیکون‌ها + دیباگ آدرس‌های ننوشته

### گزارش کاربر

1. «آدرس‌ها هم نوشته نمیشن پس دیباگ کنش» — با داده‌ی واقعی برق‌من، آدرس خاموشی‌ها در کارت‌ها/هیرو/پنجره‌ی هشدار خالی می‌ماند.
2. «متن زیر ایکون ها هم باشه» — برچسب متنی زیر آیکون‌های ناوبری باید بماند (برداشتنِ ۴٫۴٫۱ زیاد بود؛ منظور واقعی کاربر فقط حذف نقطه‌ی زیر برچسب بود).

### اصلاح ۱ — استخراج بردبارِ آدرس (`util.outage_addr`)

از این‌جا که از محیط ساخت به برق‌من دسترسی نیست (IP خارج از ایران بسته)، دیباگ به دو لایه تقسیم شد:
- **خودترمیم:** آدرس به‌ترتیب از ۲۴ کلید شناخته‌شده (`outage_address`، `outageAddress`، `blackout_address`، `address`، `place`، `location`، `area`، ...)، بعد از یک سطح تودرتو، بعد از هر کلیدی که نامش آدرس‌مانند است، و در آخر از هر رشته‌ای که محتوایش آدرس است (خیابان/کوچه/بلوار/پلاک/...) بیرون کشیده می‌شود. همه‌ی مسیرها (هیرو، کارت خاموشی، پنجره‌ی هشدار، خط تری) از همین یک تابع می‌خوانند؛ اگر واقعاً آدرس نبود، «بدون آدرس ثبت‌شده» نشان داده می‌شود — هیچ‌وقت خالی نمی‌ماند.
- **قابل‌تشخیص:** اگر هیچ کلیدی جواب ندهد، کلیدهای واقعی رکورد در `%APPDATA%\Naji\debug.log` (سقف ۲۵۶KB) ثبت می‌شود؛ کاربر این فایل را بفرستد و کلید دقیقِ سِرو در یک نسخه اضافه می‌شود. همچنین اگر آدرس از مسیر fallback پیدا شد، کلیدش در همان لاگ ثبت می‌شود.

### اصلاح ۲ — برچسب ناوبری برگشت، نقطه نه

- NavButton دوباره ۷۶×۶۲: آیکون بالا، برچسب متنی زیر آیکون.
- نشانه‌ی انتخاب همان قرص شیشه‌ای با مرز اکسنت + رنگ اکسنت آیکون/متن؛ نقطه‌ی اکسنت زیر برچسب حذف ماند (همان که کاربر خواسته بود برود).
- tooltip هم اضافه ماند — بدون مزاحمت.

**تأیید:**
- `scripts/test_addr_fallback.py` جدید — ۱۲/۱۲ (کلید اصلی/camelCase/ساده/تودرتو/نام‌مشابه/کشف-از-محتوا/نبود+لاگ/None و garbage کرش نکند/fmt با کلید جایگزین).
- warn_flow سبز؛ ui_interaction سبز؛ v4_feature ۱۵/۱۵؛ accent_sync ۲۰/۲۰؛ سموک فقط خط پایه‌ی شبکه‌ی محیط.
- رندر: `download/ui_v442/` — ۱۸ صحنه؛ برچسب‌های خانه/تنظیمات/راهنما/درباره در دو تم و EN، آدرس‌ها سرجایشان.

---

## v4.4.3 — «فاصله‌ی پایش برق‌من» برگشت (با اعمال زنده)

### گزارش کاربر

«فاصله پایش چیه / چرا نیست؟» — در ۴٫۴ هر دو «بازه» حذف شده بود؛ «چند دقیقه قبل» در ۴٫۴٫۱ برگشت اما «فاصله‌ی پایش» حذف ماند و فقط به‌شکل کاشی داشبورد دیده می‌شد — عددی بدون تنظیم، که سؤال کاربر کاملاً بجا بود.

### اصلاح

- **کارت تنظیمات:** اسپین شیشه‌ای `spin_poll` (۱ تا ۶۰ دقیقه) با برچسب «فاصله‌ی پایش برق‌من» و توضیح تفکیک دو عدد: این عدد تازگیِ لیست خاموشی‌ها را تعیین می‌کند؛ زمان‌سنج هشدار مستقل و هر ۲۰ ثانیه است.
- **اعمال زنده:** `_on_settings_changed` حالا فاصله‌ی پولر زنده را می‌سنجد (`getattr(self.poller, "interval_min", None)`) و فقط وقتی عوض شده با فاصله‌ی تازه از نو راه می‌افتد — نه هر تغییر تنظیمات، نه رستارت بیهوده. دفاعی نوشته شده تا با پولرِ ساختگی تست‌ها هم سازگار باشد.
- **شباهت ظاهری:** داشبورد «فاصله‌ی پایش» را از همان تنظیمات می‌خواند — دیگر عدد فانتوم نیست.
- storage: کامنت «ثابت داخلی» → «تنظیم کاربر (۱ تا ۶۰)».

**تأیید:**
- `scripts/warn_flow_test.py` — بخش پولر بازنویسی: مرده→راه می‌افتد؛ زنده با همان فاصله→رستارت نمی‌شود؛ زنده با فاصله‌ی عوض‌شده→رستارت می‌شود؛ خروج‌زده→هرگز.
- `scripts/ui_interaction_test.py` — هر دو اسپین (۱..۱۲۰ / ۱..۶۰) + اثر واقعی تغییر عدد ۷ روی settings.
- v4_feature ۱۵/۱۵؛ accent_sync ۲۰/۲۰؛ addr_fallback ۱۲/۱۲؛ سموک فقط خط پایه‌ی شبکه‌ی محیط.
- رندر: `download/ui_v443/` — تنظیمات با هر دو اسپین و توضیح‌ها.

---

## v4.4.4 — خلوص زبان + تایمر اعلانِ دستِ کاربر

### گزارش کاربر

چهار مورد در یک پیام: (۱) «ای متن‌هایی که قاطیش انگلیسیه با فونت داغون رو بردار… تو فارسی فقط فارسی و تو انگلیسی فقط انگلیسی» — با دو اسکرین‌شات از «GOOD TO KNOW» و «NAJI · POWER GUARD»؛ (۲) «اعلان هشدار قطعی برق چند ثانیه می‌مونه رو صفحه؟ می‌خوام کاربر زمان نشون داده شدن اعلان رو هم تنظیم کنه که مثلاً اگه بعد ۱۵ ثانیه ری‌اکشن نداد و تایمرش (تایمر اعلان، نه زمان خاموشی) تموم شد عمل خودکار رو انجام بده»؛ (۳) «لوگو فاصله‌ی پایش و تنظیمات ناقص طراحی شده»؛ (۴) «متن‌ها توی بخش لایسنس‌ها قاطی شدن و تراز نیستن».

### ریشه‌ها

- **میجرولیبل‌های ابرو (eyebrow) در هر دو زبان لاتین بودند** (مثل `"app.eyebrow": ("NAJI · POWER GUARD", …)`) و قاعده‌ی QSS شان همیشه `Space Grotesk` با `letter-spacing: 3px` — در فارسی هم متن انگلیسیِ غریبه بود، هم امضای تایپوگرافیک لاتین.
- **طول نمایش اعلان قابل‌تنظیم نبود:** «فقط خبر بده» توست ثابت ۱۸ ثانیه‌ای؛ «خبر بده + اقدام» پنجره تا لحظه‌ی شروع خاموشی باز می‌ماند (تا سقف lead دقیقه!) — پاسخ مستقیم به «چند ثانیه می‌مونه؟» عملاً «نامعلوم/ثابت» بود.
- **خط‌های «درباره/لایسنس» دوزبانه بودند** («استعداد، وزیرمتن، شبنم، Space Grotesk (مجوز OFL)») — در پاراگراف راست‌به‌چپ، توکن‌های لاتین وسط متن فارسی تراز و ترتیب نمایش را به‌هم می‌ریزند.

### اصلاح

- **خلوص زبان:** هر ۹ کلید ابرو در حالت fa فارسی شدند (ناجی · نگهبان برق / دفتر خاموشی‌ها / ترجیحات / قبض‌های من / خوب بدانید / قصه‌ی ناجی / ویژگی‌های ناجی / نشست امن / سلیقه‌ی شما). قاعده‌ی `#eyebrow` در `build_qss` زبان‌آگاه شد: fa → Shabnam ۱۱٫۵px بدون letter-spacing (فاصله‌ی حروف اتصال حروف فارسی را می‌شکند — خودِ «فونت داغون»)؛ en → Space Grotesk با spacing. `_on_lang_changed` حالا قبل از بازسازی پنجره `_apply_look()` صدا می‌زند تا QSS سراسری هم با زبان تازه شود.
- **تایمر اعلان:** تنظیم جدید `notify_seconds` (۵..۱۲۰، پیش‌فرض ۱۵) + اسپین شیشه‌ای «اعلان چند ثانیه رو صفحه بمونه؟» بین «چند دقیقه قبل» و «فاصله‌ی پایش». `WarnDialog` بازنویسی شد: حلقه حالا شمارش معکوس **تایمرِ اعلان** است (زیرنویس «تا اجرای خودکار») و صفرِ آن بدون توجه به لحظه‌ی خاموشی، اقدام پیش‌فرض را اجرا می‌کند (گیره امن ۵..۶۰۰ ثانیه). توست حالت «فقط خبر بده» هم `msecs = ثانیه‌ی کاربر × ۱۰۰۰`. متنِ «اگه تا {s} ثانیه انتخابی نکنی…» و hint مسلح («…پنجره‌ی هشدار {secs} ثانیه روی صفحه می‌مونه») زنده به‌روز می‌شوند.
- **خط‌های لایسنس/درباره:** تک‌زبانه — «اورا گلس — شیشه و شفق»، «استعداد، وزیرمتن، شبنم و اسپیس‌گروتسک — هر چهار مورد با مجوز آزاد»، «سرویس برق‌من» (نسخه‌ی EN تمام‌لاتین). نشانی‌های سایت فقط در متن‌های راهنما (جای درست‌شان) می‌مانند.
- storage: `"notify_seconds": 15` + `_int` سندیتایس (۵..۱۲۰)؛ نسخه ۴٫۴٫۴ در `main_window` و `setup.iss` (پچ باینری‌امن: BOM سالم، CRLF=۷۸/۰).

**تأیید:**
- `scripts/test_lang_purity.py` جدید — ۲۹ چک: خلوص fa/en ابروها و خط‌های درباره (با regex حروف فارسی/لاتین) + QSS زبان‌آگاه (fa بدون letter-spacing، en با آن) + کلیدهای پارامتری جدید.
- `scripts/test_notify_timer.py` جدید — ۱۶ چک: پایان تایمر ۵ ثانیه‌ای بدون واکنش → اجرای خودکارِ پیش‌فرض (dry_run) و بستن پنجره؛ کلیک «بی‌خیال» تایمر را خلع سلاح می‌کند؛ گیره ۵/۶۰۰؛ توست ۴۵ ثانیه‌ای مطابق تنظیم؛ اسپین (۵..۱۲۰) و ثبت در settings؛ hint مسلح با عدد تازه.
- warn_flow بازنویسی بخش توست (۲۰ ثانیه‌ی کاربر به‌جای ۱۸ ثانیه‌ی ثابت) — سبز؛ ui_interaction / v4_feature ۱۵/۱۵ / accent_sync ۲۰/۲۰ / addr_fallback ۱۲/۱۲ — همه سبز.
- رندر: `download/ui_v444/` — ۲۲ صحنه شامل پنجره‌ی هشدار با حلقه‌ی واکنش «۱۵ تا اجرای خودکار»، تنظیمات سه‌فیلدی فارسی، درباره‌ی تک‌زبانه و EN.

---

## v4.4.5 — سینک رنگ/تم ویندوز (باگ ریشه‌ای) + آیکون FA/EN

### گزارش کاربر

سه مورد: «تطبیق رنگ با ویندوز کار نمیکنه — من میخوام برنامه دارک/لایت بودن سیستم رو بفهمه»؛ «من میخوام برنامه رنگ ویندوز رو بفهمه و اون رو با رنگ بنفش دیفالت برنامه عوض کنه»؛ «ایکونی که برای فارسی و انگلیسی توی درباره انتخاب کردی چرا عکس ماه داره!!! باید ترکیب FA/EN باشه».

### ریشه‌ها

- **باگ ریشه‌ای رجیستری:** همه‌ی خواندن‌ها در `wininfo` با `_read_reg(None, …)` انجام می‌شد؛ یعنی `winreg.OpenKey(None, path)` — در حالی که `None` فقط برای آرگومان `computer_name` در `ConnectRegistry` مجاز است و به‌عنوان ریشه‌ی `OpenKey` استثنا می‌دهد → `accent_rgb()` همیشه `None` (بنفش امضا می‌ماند) و `apps_dark()` همیشه `False` (دارک/لایت هرگز دیده نمی‌شد). تست‌های لینوکسی این را هرگز نمی‌دیدند چون در نبود winreg همان مسیر `None` برمی‌گرداند — تست فقط «نبوفتن» را می‌سنجید، نه «خواندن».
- **آیکون کارت «فارسی و انگلیسی»:** `moonstars` (ماه و ستاره) — بی‌ربط به معنای دوزبانه‌بودن.
- (محیطی، برای تست) در Qt 6.11 ساخت قلم/`QFontDatabase` بدون `QApplication` سیگفالت می‌کند → `test_lang_purity` حالا خودش اول QApplication می‌سازد؛ برنامه‌ی واقعی از قبل در `main()` اول QApplication می‌سازد و بی‌تأثیر است.

### اصلاح

- `wininfo`: ریشه‌ی صریح `winreg.HKEY_CURRENT_USER` در همه‌ی خواندن‌ها؛ `apps_dark` اگر کلید رجیستری نبود از `QStyleHints.colorScheme` (Qt ۶٫۵+) می‌پرسد؛ گیت پلتفرم حذف شد تا مسیر رجیستری با winreg جعلی هم تست‌پذیر باشد.
- `main`: اتصال `app.styleHints().colorSchemeChanged` به `_on_windows_look_changed` (واکنش فوری به تغییر تم سیستم) + چرخ ۵ ثانیه‌ای به‌عنوان پشتوانه؛ امضای هندلر `(*_)` تا هم تایمر هم سیگنال.
- `icons`: آیکون جدید `faen` — حباب بالایی خطی با «فا» به رنگ اکسنت، حباب پایینی توپر با «EN» سفید، هرکدام دم گفتگوی خودشان (QtSvg تگ `<text>` ندارد؛ حباب/دم/متن همه با QPainter و فونت‌های باندل — وزیرمتن و اسپیس‌گروتسک — رندر می‌شوند). نسخه‌ی اول با متنِ هم‌فامِ پرش نرم در اندازه‌های کوچک خوانا نبود → بازطراحی به الگوی آیکون ترجمه با کنتراست تضمینی.
- `main_window`: کارت `about.f3` از `moonstars` به `faen`؛ `VERSION=4.4.5`.
- `setup.iss`: پچ باینری‌امن 4.4.4→4.4.5 (BOM سالم، CRLF=۷۸/۰، بدون LF تنها).

**تأیید:**

- `scripts/test_windows_look.py` جدید — ۲۶ چک با winreg جعلی: ترتیب بایت‌های هر سه منبع (AccentColorMenu قالب 0xFF|BB|GG|RR → #0078D7، AccentColor و DWM به قالب 0xAARRGGBB)، اولویت منابع و فال‌بک زنجیره‌ای، دارک/روشن، کشف تغییر ظاهر، سوارشدن اکسنت روی پالت (accent/on_accent/شفق/چیپ‌ها) و برگشت بنفش امضا با `None`، گیت سیاه، سیم‌کشی سورس (HKCU صریح، حذف ریشه‌ی None، اتصال colorSchemeChanged).
- `scripts/test_faen_icon.py` جدید — ۱۱ چک: عضویت، رندر ۲۲/۴۰، آلفای تمامِ گلیف‌های «فا» و «EN» در هر دو حباب، کشیدگی دم‌ها، سیم‌کشی درباره (faen جای moonstars).
- سوئیت کامل سبز: lang_purity ۲۹، notify_timer ۱۶، warn_flow، ui_interaction، v4_feature ۱۵/۱۵، accent_sync ۲۰/۲۰، addr_fallback ۱۲/۱۲؛ سموک فقط خط پایه‌ی شبکه‌ی محیط (IP غیرایرانی).
- رندر: `download/ui_v445/` — ۲۴ صحنه، شامل سه اکسنت ویندوز (آبی/قرمز/زرد) در دو تم که بنفش هیچ‌جا نمانده، `faen_closeup` در دو تم، و درباره‌ی فارسی/EN با آیکون تازه.
- بسته: `download/bargh-monitor-v445-windows-sync-faen-icon.zip` (۵۹ فایل، integrity OK).

---

## v4.4.6 — «۵۰ دقیقه ست کردم، واکنش نداد» + حذف لایسنس از برنامه + ممیزی آیکون‌ها

**گزارش کاربر:** چهار مورد: (۱) «میشه کلاً لایسنس‌ها رو پاک کنی از تو برنامه و فقط انتهای README گیت‌هاب باشه؟»؛ (۲) اسکرین‌شات کارت «فاصله‌ی پایش» — «لوگوش خرابه و مرتبط نیس» (آیکون refresh در اندازه‌ی کوچک مثل ماهِ شکسته دیده می‌شد)؛ (۳) «کلاً بگرد و ببین تمام آیکون‌های برنامه با محتواشون می‌خونه یا نه»؛ (۴) «نکته‌ی مهم: خاموشی رو ۵۰ دقیقه قبل ست کردم ولی الان ۴ دقیقه از اون موقع گذشته و هنوز واکنش نداده!!».

### ریشه‌یابی هشدار (بند ۴ — مهم‌ترین)

زنجیره‌ی هشدار: تایمر ۲۰ ثانیه‌ای → `_check_warnings` → لیست `_last_planned` (فقط از آخرین اسنپ‌شات موفق پولر) → `outage_datetime` → پنجره‌ی `0 < mins ≤ lead` → dedupe → `_dispatch_warning`. چهار سوراخ واقعی پیدا شد:

1. **dedupe مصرف‌شده:** هشدارِ قبلاً شلیک‌شده برای همان خاموشی، تغییرِ «چند دقیقه قبل» را بی‌صدا می‌بلعید — منطق re-arm نسخه‌ی ۴٫۳ (مقایسه‌ی lead و پاک‌سازی dedupe) در بازسازی ۴٫۴ حذف شده بود و وقتی اسپینِ lead در ۴٫۴٫۱ برگشت، هیچ‌وقت احیا نشد.
2. **پارس سخت‌گیرانه‌ی تاریخ/ساعت:** `outage_datetime` فقط دو کلید دقیق `outage_date`/`outage_start_time` را با الگوی تمام‌رشته می‌شناخت؛ هر تغییر شکل از سمت برق‌من (پسوند روز هفته، کلید camelCase، متنِ «ساعت ۱۰:۳۰») → None → رکورد **بی‌صدا** از چرخ هشدار خارج می‌شد؛ داشبورد اما رشته‌ی خام را نشان می‌داد و کاربر خاموشی را «می‌دید» ولی اعلان نمی‌آمد.
3. **داده‌ی کهنه با فاصله‌ی پایش بزرگ:** کاربرِ «فاصله‌ی پایش ۶۰ دقیقه» تا یک ساعت روی اسنپ‌شات قبلی می‌ماند؛ اگر خاموشی تازه به لیست برق‌من اضافه شده بود، پیش‌آگاهیِ ۵۰ دقیقه‌ای عملاً از دست می‌رفت.
4. **سکوتِ مطلقِ بعد از شروع:** اگر به هر دلیل (پولِ دیر، جلوبودن ساعت ویندوز) لحظه‌ی شروع رد می‌شد، `mins ≤ 0` یعنی هیچ‌وقت هیچ خبری — نه اعلان، نه ردپا.

### اصلاح

- `main._on_settings_changed` — **re-arm**: تغییرِ `lead_minutes` نسبت به `_armed_lead` → `storage.clear_warned()` + `_check_warnings()` فوری؛ شلیک همان لحظه، نه ۲۰ ثانیه بعد.
- `main._on_settings_changed` — **wake پولر**: با ثبت تنظیمات، اگر فاصله‌ی پایش عوض نشده باشد پولر یک‌بار `wake()` می‌شود تا لیست تازه شود (فقط یک‌بار به‌ازای هر امضای (lead, poll)).
- `util.outage_datetime` — **بردبار چندکلیدی + پارس بی‌سخت‌گیری**: هشت کلید دقیق تاریخ + هر کلید «date»نما؛ هشت کلید دقیق ساعت + هر کلید start/from/begin-نما؛ الگویابی `re.search` داخل هر رشته («۱۴۰۴/۶/۱۱ - سه‌شنبه»، «ساعت ۱۰:۳۰:۰۰»، ارقام فارسی، جداکننده‌های - و .)؛ شکستِ پارس با امضای کلیدهای رکورد در `debug.log` ثبت می‌شود (`when-parse-failed`).
- `main._check_warnings` — **catch-up**: خاموشی‌ای که تازه شروع شده (۰ تا ۱۵ دقیقه گذشته) و هنوز خبری نگرفته → توست فوری «قطعی شروع شد» + ردپا + چشمک تسک‌بار — **بدون هیچ اقدام سیستمی** (اقدامِ دیر خطرناک است و از ابتدا هم وعده نبوده). dedupe جداگانه با کلید `late:`.
- **قابلیت ردیابی:** هر شلیک (`warn-fire lead=… mins=… key=…`) و هر هشدار دیرهنگام (`warn-late`) در `%APPDATA%\Naji\debug.log` ثبت می‌شود — اگر باز هم جایی اعلان دیده نشد، از لاگ مشخص است شلیک کرده یا نه.
- `i18n`: کلیدهای `tray.late_title` / `tray.late_body` (دوزبانه).

### لایسنس فقط در README (بند ۱)

- `main_window._page_about`: کارت سه‌خطی «طراحی / فونت‌ها / منبع داده» (همان «بخش لایسنس‌ها» که دوبار گلایه بود) **کلاً حذف شد**؛ صفحه‌ی درباره حالا: نشان + قصه + گرید چهار ویژگی + سلب مسئولیت کوتاه.
- `i18n`: شش کلید `about.design*` / `about.fonts*` / `about.data*` حذف شدند.
- `README.md`: بخش «📜 لایسنس» در انتهای فایل حالا خانه‌ی تنهاِ اطلاعات لایسنس است: لایسنس کد (اشاره به LICENSE)، چهار فونت OFL با لینک، منبع داده برق‌من.

### ممیزی کامل آیکون‌ها (بند ۲ و ۳)

| جایگاه | قبل | بعد | دلیل |
|---|---|---|---|
| تایل «فاصله‌ی پایش» | `refresh` — قوس ناقص، در اندازه‌ی کوچک شبیه ماهِ شکسته (اسکرین‌شات کاربر) | **`timer`** — کرنومتر با دسته و عقربه | «بازه/فاصله» بی‌ابهام |
| تایل «چند دقیقه قبل» | `gauge` — گیج، با «لحظه‌ی اعلان» نمی‌خواند | **`bell`** — زنگ هشدار | زنگ = لحظه‌ی خبر دادن |
| ناوبری «راهنما» | `shield` — سپر یعنی امنیت | **`help`** — علامت سؤال | قرارداد جهانی راهنما |

بقیه‌ی آیکون‌ها بازبینی و تأیید شدند: bolt=خاموشی‌ها/هیرو/لوگو، home/gear/users=ناوبری، check=رخ‌داده، clock=پیش‌رو، bill=قبض، bell=اعلان به‌موقع (کارت درباره)، faen=دوزبانه، shield=نشست امن (کارت درباره)، sun/moon=تم، power/moon/snow=کنش‌های خاموشی/خواب/هیبرنیت، alert=هشدار، spark=تزئین. آیکون `moonstars` و `gauge` دیگر مصرف UI ندارند (در کتابخانه می‌مانند).

### تست‌ها و تأیید

- `scripts/test_v446.py` جدید — ۳۸ چک: پارس بی‌سخت‌گیری (۹ حالت رکورد)، re-arm (شلیک فوری با تغییر lead، بدون تکرار، re-arm دوباره)، wake پولر (یک‌بار به‌ازای امضا)، catch-up (اعلان late بدون هیچ اقدام قدرت، dedupe، سکوت برای گذشته‌ی دور)، آیکون‌ها (گلیف‌ها + سیم‌کشی تایل‌ها و ناوبری)، لایسنس (حذف از i18n و صفحه‌ی درباره، حضور در انتهای README).
- `test_lang_purity`: بخش ۳ به «حذف کامل کلیدهای لایسنس» تبدیل شد — سبز.
- `test_smoke` (داخل run_smoke): دو انتظارِ کهنه‌ی قبل از ۴٫۴٫۴ به‌روز شد — حلقه حالا ثانیه‌ی «تایمر اعلان» را می‌شمارد (عدد، نه «HH:MM») و تضمینِ «گذشته اقدام نمی‌کند» به سطح کنترلر منتقل شده (پوشش در test_v446).
- `scenario_test2.T3`: با شمارش پیش‌فرض ۱۵ ثانیه‌ایِ ۴٫۴٫۴، پنجره‌ی انتظار ۱۵ ثانیه‌ایِ خودِ تست رقابته بود؛ تست حالا `react_secs=6` می‌گیرد — ۴/۴ سبز.
- سوئیت کامل سبز: v446 ۳۸/۳۸، warn_flow، interaction، v4_feature ۱۵/۱۵، accent_sync ۲۰/۲۰، addr ۱۲/۱۲، notify_timer ۱۶، lang_purity، windows_look ۲۶، faen ۱۱، scenario ۷/۷، scenario2 ۴/۴؛ run_smoke روی خط پایه‌ی سابقه‌دار (فقط شبکه‌ی زنده — IP غیرایرانی).
- رندر: `download/ui_v446/` — داشبورد/درباره/راهنما در دو تم + EN + `icons_closeup` مقایسه‌ای (کهنه در برابر سه تازه).
- بسته: `download/bargh-monitor-v446-warn-fix-license-out-icon-audit.zip` (۵۹ فایل، integrity OK)؛ `setup.iss` پچ باینری‌امن 4.4.5→4.4.6 (BOM سالم، CRLF=۷۸/۰).

## v4.4.7 — «آیکون‌ها مربعی‌اند» + حذف آیکون‌های صفحه‌ی راهنما

### ریشه‌یابی
- کاشی‌های آیکونی برنامه (`IconChip` برای آمار/ردیف‌ها/گرید ویژگی‌ها و `LogoChip` برای لوگوی گرادیانی) با `drawRoundedRect` و شعاع `0.32×اندازه` رسم می‌شدند؛ این نسبت، «مربع‌گوشه» است نه دایره — در اسکرین‌شات کاربر (کاشی بنفش صاعقه‌دار) سیلوئت مربع کاملاً محسوس بود. آیکون برنامه (`assets/icon.ico` + fallback `make_icon` در main) هم مربع‌گوشه‌ی سرمه‌ای بود.
- صفحه‌ی راهنما سرِ هر یک از ۵ پرسش یک `IconChip` ۳۶px داشت؛ به باور کاربر شلوغ و زشت.
- کشف حین رندر: هاله‌ی شعاعی `LogoChip` با شعاع `0.85×اندازه` از مرز ویجت بزرگ‌تر بود و در گوشه‌های مربعیِ ویجت بریده می‌شد — لبه‌ی مربعیِ محو دور لوگوی گرد!

### اصلاح
- `IconChip`: پرش و اسپکولار روی `drawEllipse`؛ اسپکولار داخل همان دایره کلیپ می‌شود. ۹ نقطه‌ی مصرف خودکار گرد شدند.
- `LogoChip`: بدنه‌ی گرادیانی `addEllipse` کامل؛ هاله به شعاع `0.68×اندازه` داخل مرز محو می‌شود (stopها ۰٫۴→۱٫۰) — هیچ برش مربعی در هیچ پس‌زمینه‌ای.
- `ThemeButton`: ۴۰×۴۰ و قرص کامل — هماهنگ با بقیه‌ی کاشی‌ها.
- `main.make_icon` fallback: `drawEllipse(1,1,62,62)`؛ `assets/icon.ico` با `make_round_ico_447.py` بازتولید شد (بوم ۱۰۲۴ → ۲۵۶ + سایزهای ۶۴/۴۸/۳۲/۱۶، گرادیان عمودی سرمه‌ای، صاعقه کهربایی، گوشه‌ها شفاف).
- `_page_help`: قلمرو آیکون و ردیف chip حذف شد — فقط پرسش (h2) و پاسخ (body).

### تست‌ها و تأیید
- `test_v447.py` — ۲۴ چک: رندر واقعی ویجت و بررسی هندسی (گوشه‌های شفاف = دایره) برای ۵ رنگ IconChip و LogoChip ۴۸/۷۰ و ThemeButton؛ بررسی نسبتی زیر هاله برای LogoChip؛ راهنمای بدون IconChip/qrow با ۵ جفت پرسش‌وپاسخ سالم؛ fallback با Ellipse و بدون RoundedRect؛ گرد بودن QIcon و .ico (گوشه آلفا-صفر)؛ نسخه و نگاشت گلیف ناوبری.
- نکته‌ی ابزار: `QWidget.render` در PySide6 آورلود QPoint/QRegion/renderFlags می‌خواهد و بدون حذف `DrawWindowBackground` پس‌زمینه‌ی پنجره را هم می‌کشد — `grab()` تست با `DrawChildren` خالص کار می‌کند.
- سوئیت کامل سبز: v447 ۲۴/۲۴، v446 (پین نسخه به ≥ الان شل شد)، warn_flow، interaction، v4_feature ۱۵/۱۵، accent_sync ۲۰/۲۰، addr ۱۲/۱۲، notify_timer ۱۶، lang_purity، windows_look ۲۶، faen ۱۱، scenario ۷/۷، scenario2 ۴/۴.
- رندر: `download/ui_v447/` — داشبورد/درباره/راهنما در دو تم + EN + `chips_closeup` (۶ کاشی واقعی گرد) + `shape_before/after`.
- بسته: `download/bargh-monitor-v447-round-icons-help-clean.zip` (۵۹ فایل، integrity OK)؛ `setup.iss` پچ باینری‌امن 4.4.6→4.4.7 (BOM سالم، CRLF=۷۸/۰).

## v4.4.8 — «اینو حذف کن» → روشن‌شدن مقصود: حذف آیتم از صفحه‌ی درباره، نه حذف ویژگی

### ریشه‌یابی
- اسکرین‌شات کاربر کاشی سپرِ فیروزه‌ای با عنوان «هم‌رنگ ویندوز» بود = خانه‌ی چهارم گرید «ویژگی‌های ناجی» در صفحه‌ی درباره (about.f4t/f4s) — نه سوییچ تنظیمات (look.sync_accent با متن متفاوت «هم‌رنگ شدن با رنگ ویندوز»).
- توضیح کاربر: «منظور من حذف این مورد از منو درباره بود نه ویژگی‌ش» — یعنی فقط معرفیِ آن در درباره حذف شود؛ خود قابلیت سینک و سوییچ تنظیمات بماند. اجرا دقیقاً در همین حد بود.

### اصلاح
- `main_window._page_about`: خانه‌ی `("teal", "shield", "about.f4t", "about.f4s")` از گرید حذف شد — گرید ۲×۲ به ۳ خانه رسید (f1 قبض‌ها، f2 هشدار، f3 زبان).
- `i18n`: کلیدهای about.f4t/f4s حذف شدند (fa+en)؛ gلیف shield در کتابخانه‌ی آیکون‌ها ماند (مصرف UI دیگر ندارد).
- سوییچ `switch_sync` تنظیمات و موتور سینک (wininfo/apps_dark/accent) دست‌نخورده.

### تست‌ها و تأیید
- `test_v448.py` — ۹ چک: حذف کلیدها از i18n، نبودن about.f4/shield در سورس صفحه‌ی درباره، سالم‌ماندن سوییچ تنظیمات، ساخت‌شدن واقعی صفحه و نبودِ هیچ برچسب «هم‌رنگ ویندوز»، نسخه.
- سوئیت کامل سبز: v448 ۹/۹، v447 (پین نسخه ≥ شد)، v446، warn_flow، interaction، v4_feature ۱۵/۱۵، accent_sync ۲۰/۲۰، addr ۱۲/۱۲، notify_timer ۱۶، lang_purity، windows_look ۲۶، faen ۱۱، scenario ۷/۷، scenario2 ۴/۴.
- نکته‌ی محیط: venv دوباره پاک‌شده بود (سابقه‌دار) — PySide6/jdatetime دوباره نصب شد؛ libEGL فروش‌شده در vendor/qt-libs سر جایش بود.
- رندر: `download/ui_v448/` — درباره در دو تم (۳ ویژگی، بدون سپر) + داشبورد در دو تم؛ چک خودکار «هیچ برچسب هم‌رنگ ویندوز» هم داخل رندر تکرار شد.
- بسته: `download/bargh-monitor-v448-about-item-removed.zip` (۵۹ فایل، integrity OK)؛ `setup.iss` پچ باینری‌امن 4.4.7→4.4.8 (BOM سالم، CRLF=۷۸/۰).

## v4.4.9 — «اسکرول اعداد را جابه‌جا نکند» + استپر حرفه‌ای + گوشه‌های شکسته + دکمه‌ی زبان زیر تم

### ریشه‌یابی
- `GlassSpin` روی QSpinBox بومی سوار بود: QAbstractSpinBox چرخ ماوس را خودش مصرف می‌کند → اسکرول صفحه روی کنترل، مقدار را عوض می‌کرد. فلش‌های بومی زیرناحیه‌های SC_SpinBoxUp/Down با قاب QSS گوشه‌گرد تلاقی داشتند → «گوشه‌های شکسته» در اسکرین‌شات کاربر (۵ min).
- `GlassCombo` هم QComboBox بومی است: چرخ ماوس انتخاب «خاموش/خواب/خواب زمستانی» را عوض می‌کرد بدون هیچ کلیکی.
- نوار کنار فقط دکمه‌ی تم داشت؛ تغییر زبان فقط از تنظیمات ممکن بود.

### اصلاح
- `widgets._StepButton`: دکمه‌ی گرد شیشه‌ای با گلیف خطی منفی/پلاس؛ گام فوری با فشردن + تکرار خودکار (۴۳۰ms تاخیر، ۸۵ms گام)؛ در کف/سقف خاموش و کم‌رنگ می‌شود.
- `widgets.GlassStepper`: جایگزین کامل QSpinBox — چرخ ماوس در ساختارش وجود ندارد (اصلاً QAbstractSpinBox نیست)؛ عدد وسط در کپسول شیشه‌ای صاف (شعاع = نصف ارتفاع، بدون گوشه شکسته) با ارقام زبان فعلی (i18n.num)؛ API سازگار (setRange/setValue/value/valueChanged/setSuffix/setAlignment/minimum/maximum) تا main_window فقط نام کلاس عوض کند.
- سقف‌ها برداشته شد: lead 1..99999 (قبلاً 1..120)، notify 1..99999 (5..120)، poll 1..99999 (1..60) — فقط کف ۱ مانده.
- `GlassCombo.wheelEvent` → `event.ignore()`: رویداد به اسکرول‌اریا می‌رسد؛ انتخاب فقط با کلیک.
- `widgets.LangButton`: قرص ۴۰px با گلیف faen — در NavRail درست زیر دکمه‌ی تم (+سیگنال lang_toggled).
- `main_window`: اتصال lang_toggled → `_toggle_lang` (همان مسیر سگمنت زبان: ذخیره + lang_changed برای بازسازی)؛ نسخه 4.4.9.

### تست‌ها و تأیید
- `test_v449.py` — ۱۷ چک: گام دکمه‌ها، کف ۱ + خاموشی دکمه‌ی منفی، بی‌سقفی، سازه‌ی ضدرچرخ (نیستن QAbstractSpinBox)، سیگنال فقط روی تغییر واقعی، چرخِ کمبو ignore و ثابت‌ماندن انتخاب، زیرِ دکمه‌ی تم بودن دکمه‌ی زبان (هندسه‌ی واقعی بعد از show)، ذخیره‌ی زبان با کلیک، بی‌سقفی سه استپر صفحه‌ی واقعی، ثبت مقادیر.
- پین‌های کهنه هم‌تراز شد: warn_flow (max>=9999)، notify_timer (1..بی‌سقف)، ui_interaction (بی‌سقف)، test_v448 (VERSION >=).
- سوئیت کامل سبز: v449 ۱۷/۱۷، v448، v447، v446، warn_flow، interaction، v4_feature ۱۵/۱۵، accent_sync ۲۰/۲۰، addr ۱۲/۱۲، notify_timer ۱۶، lang_purity، windows_look ۲۶، faen ۱۱، scenario ۷/۷، scenario2 ۴/۴.
- رندر: `download/ui_v449/` — تنظیمات fa در دو تم + en + کلوزآپ استپر دو تم + `rail_bottom_en` (تم و زیرش زبان).
- بسته: `download/bargh-monitor-v449-stepper-wheel-safe-lang-btn.zip` (۵۹ فایل، integrity OK)؛ `setup.iss` پچ باینری‌امن 4.4.8→4.4.9 (BOM سالم، CRLF=۷۸/۰).

## v4.5.0 — «ایکون با متن تراز نیست» (سه اسکرین‌شات: تایل آمار و ردیف خاموشی)

### ریشه‌یابی (اندازه‌گیری پیکسلی، نه حدس)
- اسکریپت تشخیصی `diag_align.py`: جوهر واقعی هر ویجت جدا رندر و مرکزش با mapTo مقایسه شد.
- **OutageCard: +۶٫۰px** — چیپ ۴۲px وسط ردیف، ولی بلوک متن (چیپ روز/ساعت + آدرس) بالا-لنگر: VBox فضای اضافی را ته می‌گذاشت.
- **StatTile(en): +۳٫۰px** — ستون متن از نظر هندسی پر بود؛ اختلاف صرفاً اپتیکال بود: «2 items» حرف نزولی ندارد و جوهرش در جعبه‌ی لیبل بالا می‌نشیند. **StatTile(fa): +۰٫۵px** (بی‌نقص).
- الگو در ۸ نقطه‌ی برنامه تکرار شده بود (تایل آمار، ردیف خاموشی، ردیف قبض، گرید ویژگی‌های درباره، بنر هشدار، سربرگ راهنمای ورود، اتواستارت، هم‌رنگی).

### اصلاح
- هر ۸ ستون متن: `layout.setAlignment(nestedLayout, AlignVCenter)` — بلوک متن روی همان محور چیپ می‌نشیند.
- StatTile: جبران اپتیکال فقط برای LTR — چیپ داخل wrap با ۶px حاشیه زیرین (اندازه‌گیری‌محور: en 0.0px، fa دست‌نخورده +۰٫۵px). تعویض زبان پنجره را بازسازی می‌کند پس مقدار هنگام ساخت همیشه درست است.
- نکته‌ی ابزار: `chip.y()` بعد از wrap نسبت به والد جدید است — اندازه‌گیری باید `mapTo(root)` باشد (در diag اصلاح شد).
- نسخه 4.5.0 (نه 4.4.10 — چون مقایسه‌ی رشته‌ای پین‌های نسخه lexicographic است و «4.4.10» از «4.4.9» کوچک‌تر می‌شد).

### تست‌ها و تأیید
- `test_v450.py` — ۱۵ چک: دلتای زنده‌ی چیپ/متن در StatTile و OutageCard برای fa/en با تلورانس ±۱٫۵px + presence هر ۹ ترمیم در سورس + ساخت داشبورد.
- نتیجه: StatTile(en) ۰٫۰، StatTile(fa) +۰٫۵، OutageCard(fa) +۱٫۰، OutageCard(en) ۰٫۰.
- پین نسخه‌ی test_v449 به >= شل شد؛ سوئیت کامل سبز: v450 ۱۵/۱۵، v449، v448، v447، v446، warn_flow، interaction، v4_feature ۱۵/۱۵، accent_sync ۲۰/۲۰، addr ۱۲/۱۲، notify_timer ۱۶، lang_purity، windows_look ۲۶، faen ۱۱، scenario ۷/۷، scenario2 ۴/۴.
- رندر: `download/ui_v450/` — داشبورد fa در دو تم (تایل‌ها + ردیف‌ها) + en داشبورد + درباره + تنظیمات.
- بسته: `download/bargh-monitor-v450-icon-text-alignment.zip` (۵۹ فایل، integrity OK)؛ `setup.iss` پچ باینری‌امن 4.4.9→4.5.0 (BOM سالم، CRLF=۷۸/۰).

## v4.6.0 — «ژله‌ای بودن رو با ظرافت‌تر کن و موشنش جذاب‌تر بشه؛ کل برنامه مشکی مات بشه»

### مرجع و پورت (بدون حدس — keyframes عیناً از transitions.dev)
- مرجع: «Toggle — Thumb slides with a double burst» از transitions.dev — مدت ۳۵۰ms، منحنی `cubic-bezier(0.34, 1.35, 0.64, 1)`، keyframes ۰٪ → ۵۵٪ (اورشوت +ov1) → ۸۰٪ (برگشت −ov2) → ۱۰۰٪ نشست، کراس‌فید رنگ ریل روی ساعت مستقل، و گارد `.is-init` (در لود اول keyframe پخش نشود).
- **نکته‌ی ظرافت (درخواست این پیام)**: منحنیِ مرجع خودش ۱۶٪ اورشوتِ ضمنی دارد و روی keyframe اورشوت سوار می‌شد — جمعِ آنها «فنرِ تصادفی» می‌ساخت نه ژله‌ی ظریف. راه‌حل: منحنی بین‌فریمی مهار شد (کنترل y=۱٫۰۰؛ همان حمله‌ی سریع، بدون اورشوتِ اضافه) تا میزان ژله «فقط» با keyframes کنترل شود — اورشوتِ ناوبری ۳٫۵٪ مسیر، سوییچ ۵٪ مسیر (۱px روی ۲۰px)، برگشت ۲۸٪ همان. تست: بیشینه‌ی موقعیت دقیقاً = b+ov1 (خطای < ۰٫۰۱).
- موشنِ جذاب‌تر: نبض ژله‌ای ابعاد (۱٫۰۰ → ۱٫۰۶ → ۰٫۹۶۵ → ۱٫۰۰) در راستای سفر + کراس‌فید رنگ روی ساعت مستقل (OutQuad، ۶۰-۱۲۰ms بلندتر از سفر) — مطابق الگوی مرجع.

### پیاده‌سازی
- `widgets.py`: موتور موشن (`jelly_ease`/`jelly_pos`/`jelly_pulse`/`JellyMotion`) + میکسین `_JellyPress` (فشار ۰٫۹۴ در ۱۱۰ms، رهاشدن با دوبینسِ ریز اورشوت ۱٫۰۱۲).
- **NavRail**: پس‌زمینه‌ی ثابت دکمه‌ی فعال حذف شد؛ قرصِ فعال حالا در `paintEvent` ریل زیر دکمه‌ها رسم و با کلیک/`set_current` با دوبینس سفر می‌کند؛ گارد is-init = نبودِ موقعیت قبلی یا نمایان‌نبودن یا تغییر < ۰٫۵px؛ `resizeEvent` دوباره می‌نشاند (باگ بالقوه‌ی هندسه‌ی پیش‌ازچیدمان در لود اول).
- **Switch**: دستگیره با `jelly_pos` سفر می‌کند (X_OFF=4 → X_ON=24) + نبض ابعادی + کراس‌فید مستقل رنگ ریل (شیشه ← اکسنت) + هاله‌ی متناسب با پیشرفت کراس‌فید.
- **JellyButton**: نقاشی از مسیر `CE_PushButton` با ترانسفورم مقیاس حول مرکز — QSS و ناحیه‌ی کلیک دست‌نخورده. جایگزین در main_window (۷ دکمه)، login_dialog (۶)، warn_dialog (۲)، Segmented. ThemeButton/LangButton/NavButton/_StepButton با میکسین.
- **main_window**: `_switch_page` — محویِ ۲۰۰ms صفحه‌ی تازه با QGraphicsOpacityEffect؛ اثرِ قدیمی هرگز اثرِ سفرِ تازه را برنمی‌دارد (چک هویت اثر) و `deleteLater` با پاک‌سازی رفرنس هم‌بسته شد (باگِ کش‌شده در تستِ جابه‌جایی سریع: `RuntimeError: Internal C++ object already deleted`).
- **مشکیِ مات (theme.py)**: بوم تیره `#0d0f1c/#131630` → `#0a0a0e/#060608`؛ سطوح سرمه‌ای → خنثی (`rgba(16,16,20,…)`)؛ براقِ شیشه ۰٫۰۷ → ۰٫۰۳۵؛ شفقِ تیره (۹۶/۸۴/۶۰/۴۶/۴۲/۶۲) → (۵۲/۴۲/۲۸/۲۲/۱۸/۳۰)؛ گرادیان هیرو در تیره به v=۰٫۶۲/۰٫۵۸/۰٫۴۰ رسید (با اکسنت ویندوز هم مات می‌ماند — تست با آبی/زرد). براق‌های `widgets.py` (GlassCard/IconChip/LogoChip/HeroCard/Switch) در تیره به نصف تا یک‌سوم.
- **storage.py**: پیش‌فرض `theme/theme_mode` → dark + مهاجرت یک‌باره با نشان `matte_done` (انتخاب بعدی کاربر محترم؛ تست سه‌سناریو). main.py فال‌بک‌ها هم‌تراز شد.

### تست‌ها و تأیید
- `test_v460.py` — ۶۰ چک: ریاضیات keyframes (شروع/نشست/برگشت t=0.8، اورشوت دقیق = ov1، هرگز منفی نیست، سفر معکوس، مسیر صفر)، نبض (۱٫۰۶/۰٫۹۶۵)، منحنی مهارشده، سوییچ (is-init هر دو حالت، ساخت انیمیشن، نشست دقیق، کراس‌فید کامل، رندر)، قرص ناوبری (نشست لود اول، جابه‌جایی برنامه‌ای، سفر با کلیک واقعی، نشست، اورشوت ۳٫۵٪، رندر)، دکمه‌ی ژله‌ای (فشار جمع می‌شود با پمپ رویداد واقعی، رهاشدن به ۱٫۰، رندر در فشار)، میکسین روی ۴ کلاس، پالت مات (بوم ≤ ۱۲ لایت‌نس، خنثی بودن شیشه، براق ≤ ۱۲، شفق تیره < ۷۵٪ روشن، گرادیان ≤ ۲۱۰ حتی با اکسنت)، مهاجرت (۳ سناریو)، پنجره‌ی اصلی (محو هنگام سفر + برداشتن اثر + جابه‌جایی سریع + مسیر واقعی show_page + رندر کامل). نتیجه: ALL PASS.
- بازبینی بصری پیکسلی: `accent_yellow_dark` هیرو زردِ زیتونیِ مات (۱۱۵/۱۳۶/۱۵ → ۱۰۲/۵۷/۰)، بوم صفحه (۱۸/۱۸/۲۱) — نزدیک سیاهِ خالص.
- رندر: `download/ui_v460/` — ۱۸ صحنه: چهار صفحه در دو تم، سه اکسنت روی مشکیِ مات، EN دارک، و کلوزآپ‌های ژله (اورشوت قرص، برگشت، میانه‌ی سوییچ، فشار دکمه).
- بسته: `download/bargh-monitor-v460-refined-jelly-matte-black.zip` (۶۰ فایل، integrity OK، sha256=6da0676f…)؛ `setup.iss` پچ باینری‌امن 4.5.0→4.6.0 (BOM سالم، CRLF=۷۸/۰).

## v4.7.0 — «سفید صدفیِ مات برای لایت + خروجِ گوشه + رفع باگ سلکشن تب‌ها + ممیزی سایز آیکون‌ها»

### ریشه‌یابی باگ «سلکشن بین تب‌ها» (بازتولید و ترمیم واقعی)
- دیاگ: `scripts/diag_tabs.py` — چهار سناریو (کلیک ۶۰ms پشت‌سرهم، سه کلیک اَبَرسریق، کلیک سریع ناوبری، resize حین انیمیشن). سه سناریو اول می‌گذشت ولی **سناریوی چهارم قطعی FAIL بود**: بعد از resize وسط سفر قرص، نشانگر روی هندسه‌ی کهنه می‌نشیند (`ind=152,5/143` به‌جای `262,5/253`).
- ریشه: در `Segmented._sync_indicator` انیمیشن `QPropertyAnimation` قبلی هیچ‌وقت `stop()` نمی‌شد؛ با هر resize/retranslate/کلیکِ تازه، انیمیشنِ کهنه زنده می‌ماند و هندسه‌ی تاریخ‌مصرف‌گذشته را روی قرص می‌نوشت. (در `NavRail` توقف وجود داشت — در Segmented فراموش شده بود.)
- ترمیم: توقفِ صریح انیمیشن کهنه در ابتدای `_sync_indicator` + گاردِ «هندسه برابر = بدون انیمیشن». دیاگ بعد از ترمیم: ۴/۴ PASS. تست‌های رگرسیون در `test_v470.py` هم قفل شدند.

### تغییرات
- **صدفِ مات (theme.py)**: بوم روشن `#f0f1fa/#e6e8f5` → `#f8f5f0/#eeeae3` (گرمِ شیری)؛ متن `#1d2144/#5d6389/#9aa0c2` → `#332e2b/#6f6a66/#a7a19c` (زغالیِ گرم)؛ لبه/سایه/وینیت گرم (`124,110,92` / `166,155,138` / `196,186,170`)؛ sheen ۰٫۵۵ → ۰٫۲۸؛ شفق روشن (۸۸/۷۴/۶۲/۴۸/۴۴/۵۴) → (۵۴/۴۴/۳۸/۲۸/۳۶/۳۲) با لکه‌ی شامپاینی `233,201,160` به‌جای گلبهیِ اشباع؛ `apply_accent` آلفای لکه‌ی اولِ روشن را ۸۲ → ۵۴ می‌گذارد. براق‌های widgets (GlassCard لبه ۴۰→۲۲، IconChip ۷۰→۲۸، LogoChip ۸۰→۴۲، Switch ۴۶→۲۶، HeroCard شفق/شیمر/لبه) در روشن کف‌کرده شدند.
- **خروجِ گوشه (main_window.py)**: دکمه‌ی خروج از ردیف دکمه‌های داشبورد حذف و به انتهای سربرگ (گوشه‌ی بالا، بعد از پییل وضعیت) رفت با objectName جدید `signout` (QSS: کم‌رنگ `text3`؛ هاور رنگِ خطر + تینت danger). `update_identity` سقف elide را ۴۳۰ → ۳۶۰ داد تا سه عنصرِ سربرگ جا شوند.
- **ممیزی سایز آیکون‌ها** (منابع: Material 3 «standard icons 20/24dp»، Fluent/ویندوز ۱۶–۲۴px، راهنمای سایدبار ۲۰px): widgets — NavButton گلیف ۲۲→۲۰، ThemeButton/LangButton ۴۰→۳۶ (گلیف ۱۸/۲۰→۱۷/۱۸)، OutageCard چیپ ۴۲→۳۶، AboutUsLogo ۷۰→۵۶، EmptyState بوم ۱۲۰×۸۴→۹۲×۶۴ (بولت ۸۴→۶۰)؛ main_window — سربرگ ۴۸→۴۰، انتخاب‌قبض ۴۶→۳۸، ویژگی‌های درباره/ردیف قبض ۴۰→۳۴؛ login_dialog ۵۶→۴۸ و ۴۶→۳۸؛ warn_dialog ۴۲→۳۶. هدف‌های لمس (دکمه‌های ۷۶×۶۲ ناوبری، استپرها) دست‌نخورده.
- **هویت رنگی بخش‌ها**: `caption_block(..., tone=)` + `tone_dot()` — داشبورد `violet`، تنظیمات `teal`، راهنما `amber`، درباره `rose`؛ QSS سه tone جدید برای legendDot؛ ابروی tone-دار رنگِ fg چیپ می‌گیرد. NavRail پرده‌ی ظریف گرفت (تیره: سفید ۲۰؛ صدف: گرم ۲۶).
- **نسخه**: main_window.VERSION 4.6.0 → 4.7.0؛ setup.iss پچ باینری‌امن (BOM سالم، CRLF=۷۸/۰).

### تست‌ها و تأیید
- `test_v470.py` — ۳۷ چک ALL PASS: پالت صدفی (بوم/گرمیِ R>B/براق/لبه/متن/سایه/پاپ‌آپ)، لکه‌ی شامپاینی + کفِ شفق + آلفای ۵۴ با اکسنت ویندوز، دکمه‌ی خروج (موقعیت <۸۰px از بالا، فشرده <۴۴px، استایل signout، حذف از داشبورد، تأییدِ رفتاریِ کلیک)، باگ تب‌ها (resize حین سفر، سه کلیک اَبَرسریق، کلیک روی تب فعال)، ممیزی سایز (۹ اندازه)، tone‌ها در QSS، رندر هر دو تم + نمونه‌گیری پیکسلی گرمیِ بوم صدف.
- `test_v460.py` به‌روز شد (پینِ پالت کهنه → قفلِ ماهیت صدفی؛ نسخه ۴٫۷٫۰) — ALL PASS. اسموک `scripts/run_smoke.py`: ۱۹/۱۹ به‌جز مسدودِ همیشگیِ شبکه‌ی برق‌من (IP غیرایرانی — مکی).
- رندر: `download/ui_v470/` — ۱۵ صحنه (۴ صفحه × ۲ تم، سربرگِ کلوزآپ دو تم، EN روشن/تیره، اکسنت آبی/زرد روی صدف، سگمنت بعد از کلیک).
- بسته: `download/bargh-monitor-v470-pearl-light-tab-fix.zip`؛ صفحه‌ی دانلود وب با sha256 به‌روز شد.

## v4.8.0 — «بکگراند انیمیشنی RGB حس چیپ بودن داره — از منابع جایگزین خوب پیدا کن» + «بخش‌ها با پس‌زمینه فید میشن (لایت و دارک)»

### تحقیق از منابع (پایه‌ی تصمیم — بدون حدس)
- **Fluent 2 / Mica (microsoft)** — `learn.microsoft.com/windows/apps/design/style/mica` و `fluent2.microsoft.design/material`: «Mica is an **opaque** material… apply as the **base layer**… helps users focus…» — لایه‌ی پایه‌ی اپ دسکتاپ باید ساکن، اوپک و آرام باشد؛ پس‌زمینه‌ی زنده/چندفام با این الگو در تضاد مستقیم است.
- **Material Dark Theme / Atlassian Elevation / Muz.li Elevation Guide** — اِلیویشن در تیره = **سطحِ روشن‌ترِ خنثی** (نه رنگ اشباع که «لرزش بصری» می‌دهد)؛ و جدا نگه‌داشتن سطح کارت از بوم.
- **Grain/Noise (Framer، Peterdraw، r/web_design)** — دانه‌ی فیلم بافتِ ارگانیک/لمسی می‌دهد و گرادیانِ تخت را «لو» نمی‌کند → گرِین حفظ شد.
- نتیجه‌ی طراحی: **حذف کامل شفقِ متحرک شش‌لکه‌ای** و رفتن به «ماتِ آرام»: بوم ساکن + یک نور تک‌فام + وینیت + گرِین؛ تفکیک بخش‌ها فقط با روشنایی.

### پیاده‌سازی
- **widgets.py**: `AuroraCanvas` (تایمر ۶۶ms + شش لکه‌ی سینوسی) → **`BackdropCanvas`** کاملاً ساکن: گرادیان مات + یک نورِ تک‌فام (spec تک‌عضوی) + وینیتِ شعاعی (روش: آلفای ۶۰ لایت/۸۸ دارت از شعاع ۶۲٪ به لبه) + گرِین — همه در یک لایه‌ی کش‌شده؛ paintEvent فقط یک بلیت؛ `findChildren(QTimer) == []` قفل شد در تست. `AuroraCanvas = BackdropCanvas` به‌عنوان alias برای سازگاری importهای قدیمی/اسکریپت‌ها؛ `freeze()` بدون‌عمل نگه داشته شد. `import time` بی‌مصرف حذف شد.
- **theme.py**: `AURORA_LIGHT/DARK` از شش ۷تایی متحرک → **یک ۵تایی ساکن** `(rgb, alpha, size, x, y)`: لایت `("255,252,244", 120, 1.30, 0.50, -0.20)` استودیوییِ خنثی؛ دارت `("124,127,244", 20, 1.35, 0.50, -0.18)`. `apply_accent`: فقط نورِ دارت فامِ اکسنت می‌گیرد (تینتِ مایکا)؛ نورِ لایت همیشه خنثی می‌ماند (زردِ ویندوز لکه نزند). `aurora_spec()` تک‌عضوی.
- **پالت تیره — سطوح اِلیویت**: glass `rgba(16,16,20,0.70)` → `rgba(30,30,37,0.96)`؛ glass_strong → `rgba(37,37,45,0.98)`؛ glass_border `0.075` → `0.11` (هِیرلاین)؛ glass_edge → `rgba(0,0,0,0.55)`؛ inputها و popup هم‌تراز (`#23232c`)؛ بوم همان مشکیِ مات `#0a0a0e/#060608` ماند.
- **پالت روشن — کاغذ روی صدف**: بوم `#f8f5f0/#eeeae3` → `#f3eee6/#e9e1d4` (عمیق‌تر)؛ glass `rgba(255,253,250,0.66)` → `rgba(255,255,255,0.88)`؛ glass_strong → `0.97`؛ لبه/ورودی گرم‌تر (`114,100,82`، ۰٫۱۶/۰٫۲۰)؛ sheen ۰٫۲۸ دست‌نخورده (مات).
- **importها**: main_window / login_dialog / warn_dialog → `BackdropCanvas`؛ پایان‌خطوط LF هر سه فایل سالم ماند (راستی‌آزمایی باینری).
- **نسخه**: main_window.VERSION 4.7.0 → 4.8.0.

### تست‌ها و تأیید
- `test_v480.py` (جدید) — ۲۵ چک ALL PASS: سکونِ بوم (بدون تایمر، دو فریم بایت‌به‌بیت یکسان)، نورِ تک‌لکه‌ای/خنثی‌بودنِ لایت/آلفای ≤۲۴ دارت/بی‌اثربودنِ اکسنتِ زرد روی لایت، **تفکیک بخش‌ها** (ترکیب آلفایی glass روی بوم: دارت Δ=۱۸٫۳، لایت Δ=۱۴٫۶ ≥ آستانه)، هِیرلاین (آلفا ≥۲۶)، اوپک‌بودن کارت تیره (≥۰٫۹)، خنثی‌بودنِ فام کارت تیره، QSS هر دو پالت، رندر واقعی بوم+کارت (دارک Δ≥۱۰ و بوم L≤۱۶؛ لایت Δ≥۸ و R>B)، پنجره‌ی اصلی (بوم = BackdropCanvas، دو فریم یکسان با NAJI_NO_ANIM، رندر هر دو تم).
- `test_v460.py` (قفل ماهیت صدفی → به‌روز؛ نسخه ۴٫۸٫۰) و `test_v470.py` (پین‌های بوم/نورِ جدید) — ALL PASS. اسموک: ۱۸/۱۹ (تنها مسدودِ همیشگیِ شبکه‌ی برق‌من — IP غیرایرانی).
- رندر: `download/ui_v480/` — ۱۵ صحنه + سنجش پیکسلی تفکیک در پنجره‌ی واقعی: **لایت Δ=۳۱، دارت Δ=۲۷** (قبلاً کارت‌ها تقریباً هم‌رنگِ بوم بودند). بازبینی بصری: صدفِ عمیق با کاغذ سفید، مشکیِ مات با کارت‌های خوانا، اکسنتِ آبی فقط یک تینتِ بالای بوم (بدون RGB).
- setup.iss پچ باینری‌امن 4.7.0→4.8.0 (BOM سالم، CRLF=۷۸/۰)؛ بسته با `scripts/package_v480.py` ساخته و integrity شد.

## v5.1.0 — «دوبینسِ راستین» (شکایت: «باگ داره و همه‌چیزهایی که گفتم پیاده نشدن»)

### ریشه‌یابی — چرا کاربر «پیاده نشده» دید؟
- **دیباگ از صفر، مثل اولین‌بار**: کل تست‌ها (اسموک ۱۸/۱۹، سناریوها، تعامل) پاس می‌شدند و چیدمانِ بریفت سرِ جایش بود؛ عیب از «دیدن» نبود، از «حس» بود. در پاسِ «ظرافتِ» v4.6، منحنیِ مرجعِ transitions.dev **صاف شده بود**: `jelly_ease` با y=۱٫۰۰ ساخته می‌شد (هیچ اورشوتِ ذاتی) و `ov_scale` به ۳٫۵٪ رسیده بود. حرکتِ حاصل = سرسره؛ کاربر حق داشت بگوید دوبینس پیاده نشده.
- **باگ‌های واقعیِ یافت‌شده در همین دیباگ**:
  ۱. **کرشِ استارتاپ با settings.json خراب**: `json.load` روی فایلِ `null` (نوشته‌ی ناقصِ دیسک) مقدار None برمی‌گرداند که ValueError نیست → `data.items()` روی None → AttributeError در `storage.load()` → اپ بالا نمی‌آید. همچنین `save()` قبل از هر `load()`، رشته‌ی `"null"` روی دیسک می‌نوشت.
  ۲. **نشت آبجکت انیمیشن**: `Switch/NavRail/Segmented/_JellyPress` در هر تعامل آبجکتِ QVariantAnimation نو می‌ساختند و قدیمی فقط stop می‌شد ولی فرزندِ ویجت می‌ماند — ۳۰ تاگل = ۶۰ آبجکتِ زنده‌ی بی‌مصرف.
  ۳. **کلیکِ سریع بدون ژله**: اگر فشار و رها در یک چرخه‌ی event loop می‌رسید، `_s` هنوز ۱٫۰ بود → سفرِ رهاشدن از ۱٫۰ به ۱٫۰ = هیچ حرکتی.
  ۴. **فلاشِ تعویض صفحه**: fade با `QGraphicsOpacityEffect` روی صفحه‌ی اسکرول‌دار در باک‌اندهای GPU ویندوز ریسکِ فلاش دارد و هر سفر، رندرِ کامل صفحه + رندرِ اثر را دوبرابر می‌کرد.
  ۵. **سربارِ همیشگی**: شیمرِ هیرو (QVariantAnimation لود-بی‌نهایت) و تپشِ پییل با درایور ۶۰fps حتی وقتی صفحه/پنجره پنهان بود کار می‌کردند.
  ۶. **سربارگی سربرگ**: بریدنِ متنِ قبض‌ها با عرض ثابت ۳۶۰px بود؛ در پنجره‌ی باریکِ ۷۲۰px، جمعِ عرضِ آیتم‌های سربرگ از عرضِ پنجره می‌گذشت.

### ترمیم‌ها (با عدد)
- **حل‌گرِ بیزیرِ CSS**: `_bezier_y(x, y1)` با نیوتن (۱۰ گام) + فول‌بکِ بایسکشن (۴۸ گام) — `ymax(1.35)=۱٫۰۴۱` (اندازه‌گیری‌شده)؛ یعنی اورشوتِ ذاتیِ منحنیِ مرجع برگشت. keyframes دقیق مرجع: `0→a، 0.55→b+ov1، 0.80→b، 1→b` با `ov1=travel×OV_REF` و `OV_REF=1/14.66≈۶٫۸٪` (عین `--toggle-travel: 14.66px; ov1: 1px; ov2: 0px`).
- **پارامترها**: سوییچ = `ov_scale=OV_REF, pop=1.35` (عین مرجع؛ اوجِ اندازه‌گیری‌شده ۲۶٫۲۲ روی مقصدِ ۲۴ = ۱۱٫۱٪ اورشوت کل)؛ ناوبری = `ov_scale=۲٫۸٪, pop=1.22` (اوج ۶۸٫۹ روی ۶۶ — زیرِ دکمه‌ی همسایه نمی‌رود)؛ تب‌ها = `ov_scale=۱٫۸٪, pop=1.2`؛ دکمه‌ها = فشار ۰٫۹۶۵ (۱۱۰ms، بی‌اورشوت) و رهاشدن از `min(_s, 0.965)` با `ov_scale=۵۵٪, pop=1.35` (اوج مقیاس ۱٫۰۲۱).
- **ذخیره‌ی اتمیک**: نوشتن در `settings.json.tmp` + `os.replace` + فول‌بک؛ `load` فایلِ غیر-dict را `data={}` می‌گیرد.
- **kill_anim()**: stop + deleteLater (با گارد RuntimeError) در همه‌ی موتورهای موشن؛ بعد از ۳۰ تاگل فقط **۲** آبجکت زنده (قبلاً ۶۰).
- **FpsDriver(24)**: شیمر و تپش روی تایمرِ ۴۱ms؛ show/hide برای شروع/توقف (`set_state` نامشخص هم تایمر را می‌بندد).
- **حذف fade صفحه** → `stack.setCurrentWidget` مستقیم؛ قفلِ تست: `page.graphicsEffect() is None`.
- **سربرگ**: `_bill_text_full` + `_reelide_bill()` روی عرضِ واقعی لیبل (در resizeEvent در صف) + `LocationChip.setMaximumWidth(260)`.

### تست‌ها و تأیید
- `test_v510.py` — **۳۴/۳۴ PASS**؛ قفل‌های کلیدی: `ymax(1.35)>1.03` و `ymax(1.0)≤1.0` (منحنیِ صافِ قدیمی ممنوع)، اورشوتِ زنده در سوییچ (۲۶٫۲۲>۲۴)، ناوبری (۸۰٫۹>۷۸) و تب‌ها (۱۸۹>۵)، میکرو-فرورفتگی زیر هدف، نشست دقیق، بی‌انباشتگی آبجکت (≤۴)، فشار/رها/ضربه‌ی سریعِ دکمه، شیمرِ ۲۴fps با توقف/شروع در تعویض صفحه، پییلِ بی‌نبض در وضعیت نامشخص، سکونِ کامل با NAJI_NO_ANIM، نسخه ۵٫۱٫۰، امضای پالت `#0d0f14/#7b5cff`.
- رگرسیون کامل: `test_v460/470/480/500` (قفل‌های موشنِ کهنه به مرجعِ راستین به‌روز شدند) + اسموک ۱۸/۱۹ (تنها بلاک همیشگیِ شبکه‌ی برق‌من) + سناریوها ۷/۷ و ۴/۴ + تعامل + diag_tabs + warn_flow + accent_sync + lang_purity + faen — ALL PASS.
- رندر: `download/ui_v510/` — ۱۶ صحنه + **نوارِ ۹ فریمِ دوبینس** (حرکتِ قرص از «خانه» به «درباره» با اورشوتِ مشهود در فریم‌های میانی — مدرکِ بصری برای صفحه‌ی دانلود). تفکیک کارت/بوم: لایت Δ=۳۲، دارت Δ=۲۰؛ بومِ پایین `8,10,13` (خانواده‌ی #0D0F14).
