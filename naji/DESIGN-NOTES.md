# DESIGN-NOTES — «ناجی / Aura Glass» (نسخه‌ی ۴ — دوزبانه)

زبان بصری: **شیشه و شفق** (Glassmorphism + Aurora) — مینیمال، ترند و استودیویی.
نسخه‌ی ۴ روی همان زبان، سازوکارهای بزرگ اضافه کرد: ناوبری چهارصفحه‌ای،
دوزبانگی کامل (فا/EN + RTL/LTR خودکار)، سینک زنده‌ی رنگ و تم با ویندوز،
پایش چند-قبضی، تک‌نمونه، اسکرول‌بارهای نمایان و تایپوگرافی درشت‌تر.
خواسته‌های v3 (شیشه‌ای‌تر، چندفونت، SVG اختصاصی، بدون ایموجی) سرِ جایشان.

---

## ۱) تایپوگرافی — چهارصدایی (همه OFL، باندل‌شده در assets/fonts)

| نقش | خانواده | وزن‌ها | کاربرد |
|---|---|---|---|
| Display | **Estedad** | Medium→Black (استاتیک‌شده با fonttools از VF) | تیترها، اعداد درشت، شمارش معکوس هیرو و هشدار |
| Body | **Vazirmatn** | Regular→Black | متن بدنه، دکمه‌ها، ورودی‌ها |
| Soft | **Shabnam** | Medium/Bold | کپشن‌ها، زیرنویس‌ها، آدرس‌ها (حس گردتر) |
| Latin | **Space Grotesk** | Medium/Bold | میکرولیبل‌های لاتین با letter-spacing ۳px |

- میکرولیبل‌ها امضای تایپوگرافیک طراحی‌اند: `NAJI · POWER GUARD`،
  `OUTAGE LOG`، `PREFERENCES`، `SECURE SESSION` (QSS `QLabel#eyebrow`).
- نکته‌ی فنی: VF استعداد در Qt وزن را جابه‌جا نمی‌کرد (تست پیکسلی: صفر)،
  با `fontTools.varLib.instancer` پنج وزن استاتیک ساخته شد. اگر خانواده‌ای
  در سیستم نبود، `theme.load_fonts()` به‌صورت امن به Vazirmatn برمی‌گردد.

## ۲) شیشه و شفق

- **AuroraCanvas** (`widgets.py`): بوم پنجره = گرادیان عمیق + ۶ لکه‌ی شفق
  (radial-gradient از پیش رندرشده) که روی مسیر سینوسیِ خیلی آرام حرکت
  می‌کنند (تیک ۵۰ms، فقط وقتی پنجره دیدنی است) + گرِین فیلم (کاشی نویز
  ۹۶px با آلفای ۴..۱۴). با `NAJI_NO_ANIM=1` همه‌ی انیمیشن‌ها می‌ایستند
  (تست و اسکرین‌شات قطعی).
- **GlassCard**: پرش نیمه‌شفاف (روشن ۷۲٪ سفید / تیره ۶۶٪ سرمه‌ای) + لبه‌ی
  اسپکولار گرادیانی (سفیدِ بالا → تیره‌ی پایین) + درخش سربخش. شفقِ زیرِ
  کارت از پرش رد می‌شود و حس «فراست» می‌دهد — بدون نیاز به بلور سیستم‌عامل
  (کراس‌پلتفرم و سبک).
- **HeroCard**: گرادیان بنفش سه‌تایی + دو لکه‌ی شفق داخلی + مدار SVG
  تزئینی + **شیمر** (نوار نور مورب، چرخه‌ی ۴.۲ ثانیه) + شمارش معکوس
  استعدادِ Black با رقم فارسی.
- **StatusPill**: کپسول شیشه‌ای با «نقطه‌ی تپنده» — جایگزین کاراکتر متنی «●».
- **Switch/Segmented/IconChip**: ریل و قرص شیشه‌ای، اسپکولار و هاله‌ی اکسنت.

## ۳) آیکون‌ها — icons.py (بدون ایموجی، بدون فونت آیکونی)

۲۰+ SVG دست‌ساز روی شبکه‌ی ۲۴×۲۴ با سبک **دودوتونه**: لایه‌ی نرم
(fill کم‌شفاف) + خطوط اصلی ۱.۶..۲.۲px با سر گرد. رندر با `QSvgRenderer`.

`bolt · clock · bell · refresh · calendar · check · moon · moonstars ·
snow · power · sun · shield · signal · logout · alert · gauge · bill ·
lock · spark · orbit · gear · chevron`

- نکته‌ی فنی مهم: QtSvg مقدار `rgba()` را در ویژگی‌های fill/stroke
  **نمی‌فهمد و سیاه رندر می‌کند**؛ `svg_bytes()` رنگ‌ها را به
  `#RRGGBB` + `fill-opacity/stroke-opacity` تبدیل می‌کند.
- هر ایموجی/کاراکتر تزئینی از کل کدبیس حذف شد: هیرو، عنوان پنجره‌ی هشدار،
  پییل وضعیت، اعلان‌های تری (`📢 ⚡ ⏰ ⚠️ 📍`) — همه با SVG یا متن تمیز.

## ۴) واژگان اصولی تنظیمات (جایگزین تعابیر محاوره‌ای)

| قبل (ارزان) | بعد (اصولی) |
|---|---|
| اسلیپ | **حالت خواب** |
| هیبرنیت | **خواب زمستانی** |
| بازه‌ی بررسی / بازه زمانی | **فاصلهٔ پایش داده‌ها** |
| هشدار چند دقیقه قبل؟ | **زمان پیش‌آگاهی پیش از قطع** |
| عمل پیش‌فرض | **کنش پیش‌فرض** (+ توضیح یک‌خطی هر کنش زیر کامبو) |
| فقط عمل | **اقدام مستقیم** |
| اعلان + عمل | **اعلان و اقدام** |
| بررسی همین حالا (دکمه) | **به‌روزرسانی فوری** |
| خاموش کردن (کنش) | **خاموش کردن سیستم** |

هر کنش حالا «توضیح یک‌خطی» دارد (ACTION_HINTS) و سوییچ اجرای خودکار هم
زیرنویس توضیحی گرفت. تولتیپ‌های عددی بازنویسی شدند (مثال + توضیح اینکه
دقت هشدار به فاصلهٔ پایش وابسته نیست).

## ۵) ساختار فایل‌های UI

- `theme.py` — توکن‌ها: پالت شیشه‌ای (glass*, sheen, glow, grad1..3)،
  شفقِ روشن/تیره، چیپ‌ها (۶ رنگ: indigo/rose/amber/sky/teal/violet)،
  QSS کامل + eyebrowها، ثبت ۴ خانواده‌ی فونت با fallback امن.
- `icons.py` — قالب‌های SVG + موتور رندر (`icon_pixmap`, `icon_qicon`).
- `widgets.py` — AuroraCanvas، GlassCard، IconChip، LogoChip، ThemeButton،
  Switch، Segmented، StatTile، HeroCard، OutageCard، StatusPill،
  EmptyState، fill_outage_list.
- `main_window.py` / `login_dialog.py` / `warn_dialog.py` — چیدمان و متن.
- `main.py` / `util.py` — اعلان‌های تری بدون ایموجی.

## ۶) تست‌ها (پس از بازطراحی، بدون تغییر منطق)

- smoke: ۱۸/۱۹ (تنها تست شبکه‌ی زنده؛ برق‌من IP غیرایرانی را مسدود می‌کند —
  همان خط پایه‌ی مستند).
- scenario ۱: ۷/۷ — scenario ۲: ۴/۴ — رندر آفسکرین هر دو تم: OK.
- منطق api/power/storage/main و تمام اصلاحات دیباگِ مرحله‌ی قبل دست‌نخورده.

## ۷) اسکرین‌شات‌ها

`docs/screenshots/` به‌روز شد؛ نسخه‌ی کامل هر دو تم در
`/home/z/my-project/download/ui_v3/` (main · empty · warn · login).


## ۹) تازه‌های نسخه‌ی ۴

### ناوبری چهارصفحه‌ای
`NavRail` کپسول شیشه‌ای ۹۲px با چهار دکمه‌ی SVG بالا/برچسب پایین
(خانه/تنظیمات/راهنما/درباره) + دکمه‌ی تم در پایین ستون که چرخه‌ی
system→light→dark را اجرا و سگمنت تنظیمات را هم‌گام می‌کند.

### دوزبانگی (i18n.py)
- ~۱۵۰ رشته به‌صورت جفت (فارسی، انگلیسی)؛ هیچ متن نمایشی‌ای در کد نیست.
- `num()` ارقام را مطابق زبان می‌سازد (۰-۹ / 0-9)؛ `is_rtl()` جهت چیدمان را.
- تعویض زبان: پنجره از نو ساخته می‌شود (تمیزترین راه برای آینه‌شدن چیدمان)
  و منوی تری هم بازسازی می‌شود.

### سینک ویندوز (wininfo.py)
- اکسنت: `AccentColorMenu` (COLORREF) و `ColorizationColor` (0xAARRGGBB)
  با گاردهای اشباع؛ تم: `AppsUseLightTheme`.
- `apply_accent` علاوه بر accent، گرادیان هیرو (grad1..3)، glow، هاله‌ی
  سوییچ، چیپ indigo و لکه‌ی اول شفق را هم‌فام می‌کند.
- تایمر ۵ ثانیه‌ای Controller فقط وقتی «هماهنگ با ویندوز» روشن است نگاه می‌کند.

### کارت هیروی بازطراحی‌شده
ستون متن جدا از ستون تزئین: مدار SVG فقط در نیمه‌ی دورِ کارت (RTL: چپ)،
خط آدرس با ارتفاع رزرو ۱۸px و elide، شمارش ۴۰px استعداد، شیمر ۵٫۶s.
نشان قبض (پایش چند-قبضی) کنار چیپ «قطعی بعدی».

### تایپوگرافی درشت‌تر (درخواست «توضیحات ریز و ناخوانا»)
| نقش | v3 | v4 |
|---|---|---|
| بدنه (`body`) | ۱۳٫۵px | **۱۵px** |
| muted | ۱۳px | **۱۴٫۵px** |
| hint | ۱۲٫۵px | **۱۳٫۵px** |
| دکمه‌ی اصلی | ۱۴٫۵px | **۱۵٫۵px** |
| ورودی‌ها | ۱۴px | **۱۵px** |
| فونت پایه‌ی اپ | ۱۰٫۵pt | **۱۱pt** |

### حرکتِ نرم‌تر
شفق ۰٫۷۸× کندتر، شیمر ۴٫۲s→۵٫۶s، قرص سگمنت ۲۶۰ms OutCubic،
تیک فقط در حالت دیدنی (hideEvent تایمر را می‌ایستاند).

### اسکرول
هر صفحه در `QScrollArea#contentScroll`؛ اسکرول‌بار ۱۱px با هندل
`text3` و هاورِ اکسنت — قبلاً ۵px محو بود و پیدا نمی‌شد.

---

## ۱۰) بازنگری طراحی v4.1 — «دیزاین QA»

- **شمارش معکوس خودتنظیم**: هیرو (`_fit_px` ۳۸→۲۱px) و حلقه‌ی هشدار (۳۰→۱۶px)
  با اندازه‌گیری واقعی QFontMetrics در هر اندازه‌ی پیکسلی؛ دیگر هیچ متنی با
  مدار تزئینی یا لبه‌ی حلقه برخورد نمی‌کند.
- **دیسک داخلی حلقه**: `ring_fill` در پالت (روشن: سفید ۵۰٪، تیره: سفید ۶٪)
  بسترِ متن شمارش.
- **تراز فیزیکی داده‌ها**: `_phys_align()` = AlignStart|AlignAbsolute برای
  عنوان قبض/آدرس‌ها — Qt با AlignLeftِ ساده متنِ RTL را در LTR راست‌چین
  می‌چسباند؛ این قفلِ فیزیکی چیدمانِ دو‌زبانه را درست می‌کند.
- **اسکرول‌بار ظریف**: هندل نیمه‌شفاف `scroll` (روشن: سرمه‌ای ۳۰٪ / تیره:
  سفید ۲۰٪)، هاور اکسنت — به‌جای خاکستریِ توپرِ تمام‌قد.
- **کاشی آمار**: چیپ ۳۴px، کپشن تک‌خطی، نسبت هیرو/آمار ۴/۵.
- **پایین کارت خاموشی‌ها**: hline + راهنمای رنگی (نقطهٔ بنفش «در راهه»،
  نقطهٔ سبز «امروز») + شمارنده‌ی قبض‌ها.
- **CTA مهارشده**: عرض اصلی ۳۸۰px، وسط‌چین کنار خروج از حساب.
- **درباره**: گرید HIGHLIGHTS ۲×۲.
- **NavRail**: خط مرزی ۱px سمتِ محتوا (راست در LTR / چپ در RTL).


---

## ۱۰) نسخه‌ی ۴٫۲ — نشان «درباره ما» و کارت هیروی بی‌حلقه

- **آیکون `users`**: دو نیم‌تنه روی شبکه‌ی ۲۴×۲۴ — نفر جلو (سر r=۳٫۵ + بدنه‌ی گرد) و نفر پشت (سر r=۲٫۵ + قلاب بدنه)؛ لایه‌ی سیلوئتِ نرم زیر خطوط اصلی، مشابه تصویر مرجع کاربر برای ABOUT US.
- **`AboutUsLogo`**: قرص گرادیانی grad1→grad3 با هاله‌ی اکسنت + واژه‌نگار («درباره ما» / «ABOUT US»). فاصله‌ی حروف فقط در EN — در فارسی اتصال حروف می‌شکند.
- **کارت هیرو**: بدون مدار تزئینی؛ فقط گرادیان عمیق + دو هاله‌ی نورِ نامحسوس + شیمر. شمارش معکوس تمام عرض را در اختیار دارد.
- **NavRail «درباره»**: گلیف users — هم‌هویت با نشان صفحه.
- **حالت خالی**: مرکز ترکیب صاعقه‌ی ناجی شد (به‌جای مدار دایره‌ای) + همان دو جرقه.
